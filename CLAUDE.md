# 忍者タイプ診断アプリ — CLAUDE.md

このドキュメントはClaude Codeがこのプロジェクトを実装する際の指示書です。
「職業診断」形式の忍者版で、10問の質問に答えると12種類の忍者タイプのうち
どれに当てはまるかがカード形式で表示されるWebアプリを作ります。

---

## 1. プロジェクト概要

- **タイトル（仮）**：忍者タイプ診断
- **コンセプト**：一般的な「職業適性診断」の忍者版。共通7問で「武家系/寺社系/庶民」の
  3分類を内部判定し（ユーザーには一切見せない）、分類確定後の3問で最終タイプを
  決める、計10問構成。七方出（しちほうで）由来の7タイプ＋刺客・間者・武士・薬屋・
  薬師の計12タイプのうち1つが「診断結果カード」として表示される（6章・
  「忍者タイプ診断_新ロジック仕様書」v3準拠）。武家系の武士だけは低配点＋
  「同点勝ち不可」の特別ルールにより自然にレア職業になる（7章）
- **トーン＆マナー**：和風・時代劇風。ふざけすぎず、少し中二病的な「らしさ」を楽しめる
  雰囲気（例：「あなたは～の忍者です」ではなく「忍びの道は――**刺客**」のような演出）。
  **「お前」という二人称と「～ですか／～ます」のです・ます調は使わない**
  （2026/9/20のテスター指摘で変更。質問文は「どうする？」「何を究める？」のような
  常体、開始画面・結果画面も主語を省いた言い回し。英語版は変更なし）
- **ホスティング**：無料枠（Vercel / Netlify等）を前提。バックエンド不要、
  全てクライアントサイドで完結させる

---

## 2. 技術スタック

- **フレームワーク**：React + Vite
- **スタイリング**：CSS Modules または通常のCSS（Tailwindは使わず、和風の世界観を
  トークンベースの独自CSSで作り込む方針。詳細は4章）
- **状態管理**：React標準の`useState`/`useReducer`で十分（Redux等は不要な規模）
- **アニメーション**：CSSトランジション＋必要に応じて`Framer Motion`
  （結果カードのめくり演出など、演出の質を上げたい箇所に限定して使用）
- **画像アセット**：`/src/assets/characters/`配下にタイプごとのイラストを配置
- **バックエンド**：なし。診断ロジック・結果表示はすべてフロントエンドで完結
- **デプロイ**：Vercel想定（`vite build`の成果物をそのままデプロイ）

---

## 3. ディレクトリ構成（提案）

```
ninja-shindan/
├── CLAUDE.md
├── README.md
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── data/
│   │   ├── ninjaTypes.js       # 12タイプの定義（5章）
│   │   ├── factions.js         # 3分類(武家系/寺社系/庶民)の定義（6章、内部専用）
│   │   └── questions.js        # 共通7問 + 分類別の分岐後3問（6章）
│   ├── components/
│   │   ├── StartScreen.jsx        # タイトル・診断開始画面
│   │   ├── QuestionScreen.jsx     # 全10問共通の質問表示・選択肢UI
│   │   ├── ProgressBar.jsx        # 「第◯問」の進捗表示
│   │   ├── ResultCard.jsx         # 診断結果カード（掛軸仕立て）
│   │   ├── TypeGallery.jsx        # 結果画面下の十二タイプ一覧（分類分けは見せない）
│   │   └── decorations.jsx        # 手裏剣・題字プレート等の装飾パーツ
│   ├── logic/
│   │   └── scoring.js          # 分類判定＋最終タイプ判定（7章）
│   ├── styles/
│   │   ├── tokens.css          # デザイントークン（4章）
│   │   └── global.css
│   └── assets/
│       └── characters/         # 12タイプのイラスト（8章の命名規則で配置）
```

---

## 4. デザイン方針

このアプリ固有の「和風・時代劇画×モダンアニメ」の世界観を、AIにありがちな
テンプレ配色（生成りベース＋テラコッタ／黒背景＋ネオン一色）に寄せず、
イラストの世界観（藍・墨・紅・金）から直接トークンを起こす。

### カラートークン
```css
:root {
  --color-sumi:        #1A1A22; /* 墨・base背景（濃紺寄りの黒） */
  --color-washi:       #EDE6D6; /* 生成り・カード背景、和紙 */
  --color-beni:        #B23A2E; /* 紅・落款印、アクセント（CTAボタン等） */
  --color-kondo:       #C9A227; /* 金泥・タイトルの縁取り、演出の輝き */
  --color-ainezu:      #3E4A5C; /* 藍鼠・サブテキスト、枠線 */
  --color-text-light:  #F4F0E6; /* 濃色背景上の文字 */
  --color-text-dark:   #221F1A; /* 淡色背景上の文字 */
}
```
タイプごとの識別色は各イラストの背景色（虚無僧＝青灰、出家＝琥珀、山伏＝山緑…）
をそのまま結果カードのアクセントカラーとして流用し、統一感を持たせる。

### タイポグラフィ
- **見出し・タイプ名**：明朝体の毛筆寄りフォント（例：`Shippori Mincho`
  または`Zen Old Mincho`）。診断結果のタイプ名はこのフォントを大きく使い、
  「筆文字が浮かび上がる」演出の主役にする
- **本文・質問文**：可読性重視のゴシック体（例：`Zen Kaku Gothic New`
  または`Noto Sans JP`）
- どちらもGoogle Fontsで無料利用可能

### レイアウト・演出方針
- 1画面1質問のシングルカラム構成。スマホでの利用を主軸に設計
- 質問→回答のたびに軽いフェード/スライド遷移
- **結果画面が最大の見せ場**：暗転→和紙が広がるような背景→イラストがフェードイン
  →タイトル文字が毛筆で書かれるように現れる→最後に落款印が「ポン」と押される
  演出（Framer Motionのタイミング制御で実現）。これが本アプリの「シグネチャー演出」
- 装飾は結果画面に集中させ、質問画面はシンプルに保つ（メリハリを付ける）
- **「一つ前の問いに戻る」を全質問画面に常設**：`App.jsx`は`phase`(画面の種類)・
  `factionId`・`resultId`をそれぞれ個別のstateとして持たず、回答の配列
  （`commonAnswers`/`branchAnswers`）だけをstateにして、それ以外は毎回
  `deriveState`で導出し直す設計にしている。「戻る」は該当する配列から末尾を
  1つ取り除くだけでよく、共通7問→分岐後3問の境界をまたいでも導出済みの分類が
  古いまま残る心配がない（命令的に`setPhase`/`setFactionId`等を個別管理すると、
  戻った時にこれらがズレる恐れがあったための設計判断）。
- **選択肢の表示順をシャッフル**：「常に一番上の選択肢だけを選ぶと必ず同じ
  結果になる」という決め打ち攻略を防ぐため、`QuestionScreen.jsx`で
  `question.choices`をランダムな順序で表示する（Fisher-Yates）。判定に
  使うのは選んだ`choice`オブジェクト自体（`scores`）であり表示位置では
  ないため、シャッフルしても採点結果には一切影響しない。
  シャッフル結果は`question.id`をキーに`useRef`のMapへキャッシュし、
  QuestionScreenコンポーネントの生存期間(＝1回の診断セッション)を通じて
  保持している。「戻る」で同じ問題に再訪しても並び順は変わらない
  （毎回変わると、前回どれを選んだか分かりにくくなるため）。「もう一度
  診断する」でQuestionScreen自体が再マウントされた際はキャッシュも
  リセットされ、次の診断では改めてランダムな順序になる。
- **「一つ前の問いに戻る」ボタンの位置**：選択肢一覧の下（画面最下部）に
  配置している。当初は問題文の上に置いていたが、選択肢を選ぶ動線の
  邪魔にならないよう選択肢の後ろに移した。

### アクセシビリティ
- キーボード操作で質問の選択肢を選べるようにする（tab/enter対応）
- `prefers-reduced-motion`を尊重し、アニメーションを軽減できるようにする
- 文字サイズ・コントラスト比はWCAG AA相当を目安に

---

## 5. 12タイプ定義（`src/data/ninjaTypes.js`のもと）

| id | タイプ名 | キーワード | 性格傾向 | 移動・定住(mobility、内部専用) |
|---|---|---|---|---|
| komuso | 虚無僧 | 隠遁・静寂・内省 | 一人の時間を好み、感情を表に出さず物事を静かに観察する | hyohaku(転々) |
| shukke | 出家 | 悟り・無欲・奉仕 | 執着を捨て、人のために動くことに喜びを感じる | teiju(定住) |
| yamabushi | 山伏 | 修行・忍耐・自然 | 肉体と精神を鍛えることに価値を置き、困難を厭わない | hyohaku(転々) |
| akindo | 商人 | 社交・情報通・損得 | 人付き合いが得意で、交渉や駆け引きを楽しむ | tokidoki(時々) |
| hokashi | 放下師 | 芸・機転・身軽さ | 目立つことを恐れず、場を盛り上げるのが得意 | hyohaku(転々) |
| sarugakushi | 猿楽師 | 二面性・演技・仮面 | 状況に応じて自分を演じ分けるのが得意 | tokidoki(時々) |
| tsunenokatachi | 常の形 | 擬態・堅実・目立たなさ | 普段は目立たず、いざという時に力を発揮する | teiju(定住) |
| shikaku | 刺客 | 冷徹・単独・目的遂行 | 感情より目的を優先し、迷わず行動する | **なし(ボーナス対象外。2026/9/20に「時々」から外した)** |
| kanja | 間者 | 情報収集・洞察・裏工作 | 物事の裏側を読み取り、慎重に立ち回る | tokidoki(時々) |
| bushi | 武士 | 正義・名誉・正々堂々 | 正面から向き合うことを重んじ、筋を通す | **なし(ボーナス対象外)** |
| kusuriya | 薬屋 | 実利・商才・駆け引き | リスクとリターンを見極め、実利を取りに行く | hyohaku(転々) |
| kusushi | 薬師 | 冷静・分析・専門知識 | 状況を分析し、知識で周囲を助ける役に回る | teiju(定住) |

各タイプには結果画面用に以下の情報も持たせる：
- `name`（表示名）／`nameEn`（ローマ字、ファイル名等に使用）
- `description`（診断結果として表示する2〜3文の説明文。作成済み・変更しない）
- `accentColor`（4章のイラスト背景色に対応する16進カラー）
- `imagePath`（8章の命名規則に対応する画像パス）
- `mobility`（内部専用。上表の右端列。'teiju'/'tokidoki'/'hyohaku'。7章「移動・定住軸」の
  ボーナス判定にだけ使い、画面・DOM・console・storage・URLには出さない。
  **武士と刺客は意図的に持たない**）

---

## 6. 質問設計（共通7問 + 分岐後3問 = 全10問）

**「忍者タイプ診断_新ロジック仕様書」（最終版v3・職業適性診断ライクな設問版）に
全面準拠**。旧・2陣営制(目立つ/目立たない軸＋グループ決定3問＋最終1問)は
この仕様書により完全に置き換えられ無効（旧版の内容は前バージョンの本ファイルの
履歴を参照）。

- **共通7問**（`COMMON_QUESTIONS`）：全員に同じ4択を出す。「武家系(buke)／
  寺社系(jisha)／庶民(shomin)」という3分類のいずれかに内部的に加点する
  （**この分類名はユーザーに一切見せない** — 質問文にも進行中の画面にも
  結果画面にも出さない。1章参照）。忍者用語（任務・潜入・忍び込む等）を排し、
  「新しい仕事」「旅先」「贈り物」など一般的な職業適性診断に見える文言にしてある。
- **分岐後3問**：7問終了時点で確定した分類に応じて、`BUKE_QUESTIONS`/
  `JISHA_QUESTIONS`/`SHOMIN_QUESTIONS`のいずれかを3問出題し、最終タイプを決める。
  武家系・寺社系は毎回同じ固定3問、庶民も同じ固定3問（クラスターによる
  事前分岐は行わない、7タイプ直接のフラット得点制）。

### 3分類とタイプの割り当て（`src/data/factions.js`）

| 分類(内部id) | タイプ数 | 内訳 |
|---|---|---|
| 武家系(buke) | 3 | **武士（低配点＋同点勝ち不可でレア化）**／虚無僧／薬師 |
| 寺社系(jisha) | 2 | 出家／山伏 |
| 庶民(shomin) | 7 | 常の形／商人／薬屋／猿楽師／放下師／間者／刺客 |

分類名(buke/jisha/shomin)は開発者向けの内部コード名としてのみ使う。
`ResultCard.jsx`・`TypeGallery.jsx`のどちらにも分類名・グループ名は一切出さない
（旧版にあった結果画面の「◯◯陣営の忍び」という一文は、この方針により廃止した）。

### 共通7問（3分類を決める）

1. **新しい仕事を任された。まず何をする？**
   A. まず資料や過去の事例を調べ、確実な方法を考える（武家系+2）
   B. 心を落ち着かせ、自分の調子を整えてから取りかかる（寺社系+2, kamoku+1）
   C. 周囲の人に話を聞き、必要な情報を集める（庶民+2, shakou+2／axis:hyohaku）
   D. 必要になりそうな物や手順を、先に整えておく（庶民+2, 武家系+1／axis:tokidoki）

2. **旅先で道に迷ったら、どうする？**
   A. 地図や案内を確認し、確実な道を探す（武家系+2／axis:teiju）
   B. いったん立ち止まり、焦らず周囲の状況を見直す（寺社系+2, kamoku+1／axis:hyohaku）
   C. 近くの人に声をかけ、道を教えてもらう（庶民+2, shakou+2／axis:tokidoki）
   D. 周囲をよく観察し、自分で判断できる材料を集める（庶民+2, 寺社系+1／axis:hyohaku）

3. **初対面の相手とは、どのように接する？**
   A. 礼儀を大切にし、知識や誠実さで信頼を得る（武家系+3／axis:teiju）
   B. 必要以上に語らず、相手と静かな距離を保つ（寺社系+2, kamoku+2／axis:hyohaku）
   C. 気軽に話しかけ、自然に打ち解ける（庶民+2, shakou+2／axis:hyohaku）
   D. 相手の表情や様子をよく見てから、接し方を決める（庶民+2, 武家系+1, kamoku+1／axis:tokidoki）

4. **困っている人を見かけたら、どうする？**
   A. 状況を整理し、自分にできる現実的な助け方を考える（武家系+2, kamoku+2）
   B. まず相手の気持ちを落ち着かせ、安心できるようにする（寺社系+2, kamoku+1）
   C. 自分から声をかけ、できることがあれば手を貸す（庶民+2, shakou+2）
   D. 直接手を出す前に、何が必要なのかを見極める（庶民+2, 武家系+1, kamoku+1／axis:tokidoki）

5. **贈り物を選ぶなら、どうする？**（職務と無関係な日常設問）
   A. 実用的で質の良いものを、じっくり吟味する（武家系+2／axis:teiju）
   B. 相手の無事や幸せを願えるものを選ぶ（寺社系+2／axis:tokidoki）
   C. 相手が喜ぶ姿を想像しながら、楽しんで選ぶ（庶民+2／axis:hyohaku）
   D. 値段と価値の釣り合いを考え、長く使えるものを選ぶ（庶民+2, 寺社系+1）

6. **思いがけないトラブルが起きた。どうする？**
   A. 状況を整理し、筋道を立てて対処する（武家系+2）
   B. 心を乱さず、まず落ち着いてから動く（寺社系+2）
   C. 周囲と相談しながら、その場をうまく収める（庶民+2, shakou+2／axis:tokidoki）
   D. その場で最も安全な方法を、素早く選ぶ（庶民+2, 寺社系+1／axis:hyohaku）

7. **あなたが「仕事のできる人」だと思うのは、どんな人？**
   A. 知識や技術を着実に積み重ねている人（武家系+2）
   B. どんな状況でも、自分を律することのできる人（寺社系+2／axis:hyohaku）
   C. 周囲とうまく関係を築き、場をまとめられる人（庶民+2, shakou+2／axis:tokidoki）
   D. 先回りして準備し、状況に応じて動ける人（庶民+2, 武家系+1, kamoku+2／axis:hyohaku）

`shakou`（社交性）／`kamoku`（寡黙さ）という裏スコアも同時に貯める。庶民に
分岐した場合のみ最終判定で使う（7章「庶民の判定」参照）。shakouはC選択肢に
（c1c+2, c2c+2, c3c+2, c4c+2, c6c+2, c7c+2。c5cは0）、kamokuは上記の選択肢
（c1b+1, c2b+1, c3b+2, c3d+1, c4a+2, c4b+1, c4d+1, c7d+2）に付く
（2026/9/20に、質問と点数の一覧(Excel)を確認したユーザーの指摘で再配分。
旧: shakouは全7問のC+1、kamokuはc2d/c3d/c4d/c6dの各+1。7章参照）。

**点数の上限は3ポイント（2026/9/21のユーザー方針）**：質問に付ける全ての点数
（分類点・社交性・寡黙さ・分岐後の各タイプの点）は3以下にする。新しい点数を付ける
ときも3を超えないこと（`questions.js`冒頭のコメントにも明記。確認する場合は、全選択肢の
点数の最大値を出して3以下であることを確かめる）。

**配点調整（案A・2026/9/20）**：選択肢Dは全問「庶民+2・武家系+1」（c2・c5・c6のみ
「庶民+2・寺社系+1」。c2d・c5dはユーザー指摘で武家系+1を寺社系+1に置換した）に
変更した（旧: 武家系+1・庶民+1）。Dの`dominantKey`は
全問`shomin`（`scores`のキー順もshominを先頭にしておくこと。タイブレークに
影響する）。A〜Cの主配点・shakouは従来どおり（kamokuの付与先は後述の
平等化調整で変更）。理由は7章「出現率」参照
（共通7問で約39%が武家系に進み虚無僧・薬師で山分けする構造を緩和するため）。
Cの庶民ポイントは「社交的な人＝庶民」とは限らないため強めていない。
Dの武家系+1は、Dの文言（先回りして準備する等）が賢さを含むため外さない。

**移動・定住タグ（`axis`、内部専用）**：各選択肢に上記の `axis:` を付けてある
（teiju=定住する／tokidoki=時々定住する／hyohaku=ずっと転々とする／なしは
タグ無し）。1回選ぶごとに該当区分へ+1。文言は一切変えていない。7章
「移動・定住軸」で使う。タグの読み方：teijuは資料・前例を調べる、地図で確実な
道を探す、礼儀で長く信頼を築く、質の良い物を選ぶ、着実に積み重ねる等。
tokidokiは人に話を聞く・相談する、様子を見て接し方を変える、必要な物を
見極める、相手の喜ぶ顔を想像する等。hyohakuは旅支度を先に整える、自分で
観察して判断する、深入りせず距離を保つ、気軽に打ち解ける、素早く安全策を
選ぶ、自分を律する等。

### 分岐後3問：武家系（武士／虚無僧／薬師）※v3.1で全面改訂

武士は「服部半蔵のように、時間をかけて信頼を勝ち取り武士として認められた者」
というコンセプト（刀や兜など武士を直接示唆する表現は避け、"同じ相手・同じ関係
にじっくり向き合う一貫性"を識別軸にする）。4択に統一し、Dは虚無僧・薬師どちら
にも寄らない「器用貧乏」枠（虚無僧+2・薬師+2の均等加点。当初+1/+1だったが
7章「武士のレア化」のとおり+2/+2に引き上げた。2026/9/20のユーザー指摘で、問1の
Dは虚無僧0（薬師+2のみ）、問2・問3のDは武士+1を追加した）。

```
問1　忍びとして潜入するなら、どのように？
A. 何度も通い、時間をかけて信頼を得てから中に入る（武士+1）
B. 物静かに、心を落ち着けたまま歩み入る（虚無僧+2）
C. 相手の様子を見極めながら、慎重に近づく（薬師+2）
D. その場の状況に応じて、やり方を柔軟に変える（薬師+2）

問2　休みの日を、どう過ごす？
A. 決まった相手と、じっくり関係を築く時間に使う（武士+1）
B. 静かに過ごし、心を整える時間に充てる（虚無僧+2）
C. 本を読んだり、調べ物をして過ごす（薬師+2）
D. その日の気分で、過ごし方を変える（虚無僧+2, 薬師+2, 武士+1）

問3　空いた時間に、何を究める？
A. 人との信頼関係を、じっくり時間をかけて築くこと（武士+3）
B. 己の内面や、人としての在り方（虚無僧+2）
C. 知識や、物事の理屈（薬師+2）
D. その時々で、興味のあることを幅広く（虚無僧+2, 薬師+2, 武士+1）
```

武士の選択肢Aは、2026/9/20の平等化調整でいったん+1→+2に引き上げた後、同日の
再調整で問1・問2が+1、問3が+3になった（武士だけ「同点勝ち不可」の特別ルールが
あるため、なお最レア。7章参照）。
判定は7章参照（武士だけ通常のタイブレークと異なる特別ルールがある）。

### 分岐後3問：寺社系（出家／山伏）※v3.1で全面改訂

七方出の変装ネタ（出家＝托鉢僧、山伏＝修験者）と、職業カードの視覚要素
（数珠・袈裟、法螺貝・山）を反映した文言に変更したが、2026/9/20にユーザーが
「あからさますぎる」と指摘した9か所は、托鉢・読経・数珠・法螺貝・山伏の姿といった
答えが見え透く語を避け、日常の言い回しに直した（出家＝奉仕・静けさ、山伏＝
険しい道・修行・仲間、という対応は変えていない。指摘のなかった問2B・問2D・問3Dは
そのまま）。

```
問1　忍びとして潜入するなら、どのように？
A. 見返りを求めず、困っている人を助けながら入り込む（出家+2）
B. 争いを避け、穏やかな態度で人々の中へ入り込む（出家+2）
C. 険しい道のりも自分の足で越え、正面から訪れる（山伏+2）
D. 厳しい道を仲間とともに歩み、一団として入り込む（山伏+2）

問2　心を乱されそうなとき、どうする？
A. 静かに目を閉じ、自分の心と向き合う（出家+2）
B. 人々の苦しみと、己の役目を思い返す（出家+2）
C. 深く息を整え、体に意識を向けて落ち着く（山伏+2）
D. あえて厳しい山に分け入り、自分を奮い立たせる（山伏+2）

問3　忍びとして「強さ」を示すなら？
A. 人のために、己を律して動く（出家+2）
B. 静けさの中で、欲や迷いに振り回されぬ心を示す（出家+2）
C. 苦しい道のりでも、前に進み続ける（山伏+2）
D. 険しい岩場を歩き通し、鍛え抜いた身体で示す（山伏+2）
```

判定：3問合計で出家 vs 山伏の高い方。同点は問3→問1の順でタイブレーク。

### 分岐後3問：庶民（常の形／商人／薬屋／間者／刺客／放下師／猿楽師）※v3.1で全面改訂

クラスターによる事前の足切りは行わない。常の形は3問すべての選択肢Aとして
毎回登場し、他の6タイプはB/C/Dに振り分ける。職業カードの持ち物（そろばん・
薬箱・刃・面等）を直接文言に入れると答えが見え透くため、共通7問と同じ
「日常の一場面」フォーマットに統一し、婉曲な表現に留めている。
（2026/9/20：「お前が得意なのは？」「何を究めますか？」等の二人称・です・ます調を
常体に直した。武家系の問2・問3も同様。スコア・選択肢は不変）

```
問1　忍びとして潜入するなら、どのように？
A. 現地の人に紛れる（常の形+3）
（旧文面「一般庶民になりきって、紛れ込む」は、内部の分類名「庶民」が画面に出て
いたため、ユーザー指摘で2026/9/20に変更した）
B. 商いを口実に、堂々と入り込む（商人+2）
C. 誰にも気づかれず、静かに近づく（間者+3）
D. 興行の許可を得て、人前で技を披露しながら近づく（放下師+2）

問2　忍びとして、得意なのは？
A. 何者でもない顔で、居続けること（常の形+3）
B. 薬箱を開き、効能を説いて売り歩くこと（薬屋+2）
C. 気配を断ち、一息で事を成すこと（刺客+3, 間者+1）
D. 面をつけ、別人に成りきり演じきること（猿楽師+3）

問3　空いた時間に、何を究める？
A. 特に何かを究めようとは思わない（常の形+2）
B. 物欲を高められるよう、豆知識を増やす（商人+2, 薬屋+2）
C. 精神統一する（刺客+1, 間者+1）
D. 人を惹きつける技や、表現の仕方（放下師+1, 猿楽師+1）
```

問3のCは、間者・刺客にもかかわらず一見「信仰心」を思わせる表現（精神統一
する）にすることで、寺社系の設問と紛らわしくし、選択肢から結果を逆算しにくく
している。

**バランス修正（v3・重み付け導入後の総当たりで発覚）**：問1・問2は各ペア
（商人/薬屋、間者/刺客、放下師/猿楽師）のどちらか一方が「主+2」・もう一方が
「従+1」の配点だが、v3.1では**問3だけ主従を設けず両方に+1ずつの均等配点**
に変更した。以前は問1・問2どちらも商人・間者・放下師が「主+2」を2回、
薬屋・刺客・猿楽師が1回しか得られない構造だったため、総当たり検証で
「薬屋(1.14%)・刺客(0.33%)が武家系のレアタイプである武士(1.4474%)より
出現しにくい」という逆転が発覚していた。問3を均等配点にしたことで、
薬屋は4.14%まで改善し武士(2.44%)を上回るようになった。ただし**刺客
(1.21%)は今回の修正後も武士(2.44%)よりわずかに低いまま残っている**
（原因は共通7問のshakou/kamoku付与頻度の非対称性— shakouは全7問のC
選択肢に、kamokuは7問中4問のD選択肢にしか付与されないため、庶民に
辿り着く回答パターンではshakouの方が平均して多く貯まりやすく、
shakou恩恵を受ける商人・薬屋・放下師・猿楽師の方が、kamoku恩恵を受ける
間者・刺客より底上げされやすい構造的な非対称性。詳細は7章「出現率」参照）。
※この節の数値はv3.1時点のもの。平等化調整後（2026/9/21）は刺客5.44%・
薬屋8.04%となり、ともに武士(3.23%)を上回っている（7章参照）。

**平等化調整（2026/9/20）**：上の配点は、12タイプの出現率をなるべく均等にする
ための再調整後の値（7章「平等化のための再調整」参照）。書き順にも意味がある
点に注意：問3のB・Cは、同点時のタイブレーク（`dominantKey`＝scoresの最初に
書かれたキー）で優先されるタイプを先頭に書いている（Bは商人が先、Cは刺客が先、
Dは放下師が先）。scoresのキー順を入れ替えると出現率が変わる。

判定は7章「庶民の判定」参照（shakou/kamokuの加算あり）。

### 結果画面下の十二タイプ一覧（`TypeGallery.jsx`）

分類名(武家系/寺社系/庶民)・グループ名は一切出さない、フラットな1列リスト
（1章の最重要方針に基づく）。各カードの説明文（`description`）は従来通り
2〜3文表示のまま。`ninjaTypes.js`の`description`等の表示データは
この新ロジック移行でも一切変更していない（内部専用の`mobility`を追加したのみ）。

## 7. 診断ロジック（`src/logic/scoring.js`）

### 第1段：分類の判定（`resolveFaction`）

1. buke/jisha/shominのスコアを0で初期化し、共通7問の回答ごとに加点
2. 最高得点の分類を採用
3. **同点処理**：「第7問の回答」→それでも同点なら「第1問の回答」の順で
   タイブレーク（新ロジック仕様書4章の指定通り。第1問だけで決めると影響が
   強くなりすぎるため、旧版の「第1問のみ」から変更）。タイブレークに使う
   「その回答の分類」は`dominantKey`（scoresオブジェクトの中で最も加点が
   高いキー、同点なら最初に見つかったキー）で求める
4. `shakou`/`kamoku`の累計値も同時に返す。武家系/寺社系に分岐した場合は
   使われず、庶民に分岐した場合のみ第3段の判定で使う
5. 移動・定住軸の`axisWinner`も返す（下記「移動・定住軸」参照。分類の判定
   そのものには使わない）

### 第2段：分岐後3問の判定 — 寺社系（`resolveBranchType`）

3問合計のフラット得点比較（軸ボーナス加算後）。同点は「問3(最後の問)」→
「問1(最初の問)」の順でタイブレーク（判定に使う「その回答のタイプ」も
`dominantKey`で求める）。

### 第2段：分岐後3問の判定 — 武家系、武士のレア化（`resolveBukeType`）

v3.1でBUKE_QUESTIONSが3択→4択（D＝虚無僧・薬師どちらにも寄らない
「器用貧乏」枠）に変わった。当初は武士(bushi)が+1、虚無僧(komuso)・薬師
(kusushi)が+2という配点差だったが、素直に「同点は問3→問1でタイブレーク」を
武士にも適用すると、際どい同点でも武士が勝ててしまい、「自然にレア」という
仕様書の意図が成立しない。
**2026/9/20に武士の選択肢を+2へ引き上げた**（「武士は少し出やすくなっても
よい」というユーザー判断。全体の出現率を均等に近づける調整の一環。同日の再調整で
問1・問2のAは+1に戻り、問3のAだけ+3になった）。
「同点勝ち不可」ルールは維持しているので、武士が結果になるのは武士の合計が
虚無僧・薬師の合計をどちらも**単独で**上回るとき（武士の選択肢を複数回選んだ
場合がほとんど）だけで、約3.2%と12タイプ中もっともレアなまま。以下の
「Dの配点」「タイブレークの不具合」の説明は、武士が+1だった時点（〜9/19）の
経緯である。

そのため、**武士だけは同点勝ちを許さない特別ルール**にしている：
- 素点比較で武士が単独最高点（＝他の2タイプの合計をどちらも上回る）場合の
  みが、武士が結果になる。**虚無僧・薬師の合計は軸ボーナス加算後の値で比較する**
  （武士自身はボーナスを受けない）
- 同点に武士が含まれる場合は、武士を候補から除外してから残りの候補で
  （必要なら問3→問1のタイブレークを経て）虚無僧/薬師のどちらかに決める
- 虚無僧・薬師同士の同点タイブレークはこれまで通り問3→問1

**Dの配点（`komuso: 2, kusushi: 2`）**：「12タイプ中、武士を最もレアな
タイプにしたい」という要望を受けて調整した値。当初Dは`{ komuso: 1,
kusushi: 1 }`だったが、この配点だと「3問中2問が武士・残り1問がD」という
組み合わせで武士(素点2)が虚無僧・薬師(各1点)を単独で上回ってしまい、
武士が同点を介さずに勝ててしまう抜け道になっていた。Dの配点を2/2に
上げることで、この組み合わせを「武士・虚無僧・薬師の3すくみの同点」に
変え(3問すべて武士を選んだ場合のみに武士の到達条件を絞り込む。総当たりで
全64通り中1通り＝1.5625%まで下がることを確認済み)。

**タイブレークのフォールバック不具合の修正**：上記のDの配点調整を検証する
過程で、`breakTie`（旧・各関数に重複していたインラインのタイブレーク処理）に
バグを発見した。「最後の問の回答のdominantKey」がwinnersに含まれない場合、
無条件に「最初の問の回答のdominantKey」を返していたが、**その値がwinners
に含まれているかを確認していなかった**。具体的には、武士・虚無僧・薬師が
3すくみで同点になり武士を除外した後の候補が{虚無僧, 薬師}のケースで、
最初と最後の質問がどちらも「武士」の選択肢（dominantKeyは当然'bushi'）
だった場合、除外されたはずの'bushi'がそのまま返ってしまっていた（全64通り
中1通りでこの不具合が発生し、武士の出現率が意図した1.5625%の2倍である
3.125%まで上振れしていたことを実際に総当たりで確認）。`breakTie`という
共通関数に切り出し、`winners`に実在するかを都度確認するよう修正して
`resolveFaction`/`resolveBranchType`/`resolveBukeType`/`resolveShominType`
の4箇所すべてに適用した（同じ抜け道が他の分類にも存在しうる構造だったため、
buke限定のパッチではなく共通処理として直した）。

### 第3段：庶民の判定（`resolveShominType`）

クラスターへの事前分岐を行わず、7タイプを直接フラット得点制で判定する。

1. 分岐後3問の素点合計に対し、共通7問で貯まった`shakou`/`kamoku`を加算する：
   - 商人・薬屋・放下師・猿楽師の得点 += shakou合計
   - 間者・刺客の得点 += kamoku合計
   - 常の形の得点 += shakouとkamokuの**小さい方**（2026/9/21に変更。社交的にも寡黙にも
     偏らない「バランスのよい人」ほど有利。旧: 加算なし）
2. さらに移動・定住軸ボーナス（下記）を足し、その合計で最高得点のタイプを採用
3. **同点処理**：「問3→問1」の順でタイブレーク（タイブレーク自体はshakou/kamoku
   加算前の生スコアの優劣で判定する）

これにより、共通7問で終始「愛想よく・賑やかに」系の選択肢（shakouタグ付き）を
選び続けた人が庶民分岐で急に間者・刺客になる、あるいはその逆、といった矛盾を
抑えつつ、常の形はどちら寄りの人にも公平に選ばれ得る「広く当てはまる標準形」
という位置づけを保つ。

### 移動・定住軸（`axisWinner`と軸ボーナス、2026/9/20追加）

「移動・定住軸 実装指示書」（配点調整案A＋軸）に基づく。共通7問の選択肢に付けた
`axis`（6章）を数え、最終タイプ判定に一律のボーナスを足す仕組み。

1. `resolveFaction`が`teiju`/`tokidoki`/`hyohaku`を0で初期化し、選んだ選択肢の
   `axis`ごとに+1する。最大値が**単独首位**ならその区分が`axisWinner`、
   **同点または全て0なら`null`**（ボーナスなし）。戻り値に`axisWinner`を追加
   （`App.jsx`の`deriveState`が毎回回答配列から導出し直して`resolveFinalType`
   に渡す。stateは増やしていない）。
2. 最終判定（3分類共通）で、`mobility === axisWinner`のタイプの合計点に
   `AXIS_BONUS = 1`（一律。貯まった点数に比例させない。指示書は2だったが、
   2026/9/20の平等化調整で1に下げた）を足す。庶民は
   shakou/kamoku加算の**後**に足す。武家系は武士に**足さない**（`mobility`なし）
   うえ、武士の勝利条件（虚無僧・薬師の合計をどちらも単独で上回る）はボーナス
   加算後の合計で比較する。寺社系は出家/山伏の合計に足す。
3. タイブレーク（`breakTie`、`dominantKey`）は従来どおり生の配点で行い変更なし。
4. `mobility`の割り当て（5章）：定住=薬師・出家・常の形／時々=商人・猿楽師・
   間者／転々=虚無僧・山伏・放下師・薬屋。**武士と刺客は対象外**（刺客は2026/9/20に
   「時々」から外した。下記「追加調整その2」参照）。`scoring.js`は開発時
   (`import.meta.env.DEV`)に、`NO_MOBILITY_TYPE_IDS`（武士・刺客）以外のタイプの
   `mobility`付け忘れを検知する。
5. **axis名(teiju/tokidoki/hyohaku・定住/時々/転々)は内部分類名と同じく、
   画面・DOM属性・console・storage・URLには一切出さない**（実診断を日英で完走し、
   ページ全体・全属性・localStorage/sessionStorage・URL・consoleに出ないことを
   2026/9/20に確認済み）。

### 平等化のための再調整（2026/9/20）

案A＋軸ボーナス（AXIS_BONUS=2）を反映した直後の出現率は、薬師12.15%／放下師12.14%／
商人11.87%…刺客2.25%／武士0.37%（表は下の「旧値」参照）で、刺客・間者・猿楽師が出に
くく、上位との差が大きかった。「なるべく平等にしたい。武士のレア度は少し変わっても
よい」という要望を受け、**質問文・選択肢文・A〜Cの主配点・タイブレークのコードは
変えず**、配点（内部の数値）だけを再調整した。方法は、配点を動かせる総当たり
シミュレーター（scoring.jsのロジックを再現し、現行値を小数第3位まで再現できることを
確認済み）で、目標（武士約3%、他の11タイプは約8.8%ずつ）との差が小さくなる組み合わせを
焼きなまし法で探索した（実テスターの選択傾向で重み付けした分布も併せて目的関数に
入れ、特定の選択肢に偏る人でも大きく崩れにくい案を選んだ）。変更点：

- `AXIS_BONUS` 2→**1**（scoring.js）
- 武家系分岐：武士の選択肢を+1→**+2**（3問とも）
- 共通7問のkamoku（寡黙さ）の付与先を再配分：c2d/c3d/c4d/c6dの各+1 →
  c1b+1, c2b+1, c3d+2, c4b+2, c7d+2（間者・刺客が出にくい偏りの是正。c4bは後述の追加調整で+1に戻した）
- 庶民分岐：問1B=商人3・薬屋2、問2B=薬屋2のみ、問2D=猿楽師3・放下師1、
  問3B=薬屋1・商人1（記述順を薬屋が先に）、問3C=刺客2・間者2（記述順を刺客が先に）

内部分類の到達率は変わらない（武家系23.41%／寺社系16.33%／庶民60.25%）。

**追加調整（2026/9/20、ユーザー承認）**：共通問4の選択肢B「まず相手の気持ちを落ち着かせ、
安心できるようにする」のkamoku（寡黙さ）を+2→**+1**に変更した。この文面は気遣いで
寡黙さとは合わず、均等化の探索が付けた+2は数値合わせだったため（元の指示書では0）。
刺客を「武士に次ぐレア」にする検討の中で、ユーザーが対象の文面を確認したうえで
承認した**この1か所のみ**を反映した。効果：刺客8.85→8.24%、間者8.69→7.89%
（他は微増）。この時点では刺客は2番目にレアな状態には未達（8.24%は出家・山伏8.17%、
商人8.01%、間者7.89%より多い）で、続けて下記の追加調整その2で解決した。

**追加調整その2（2026/9/20、ユーザー承認）**：刺客を「武士に次ぐレア」にするため、
定住・転々のポイントの側で調整した。検討結果：ボーナス値やタグを動かすだけでは、
刺客が「時々」の区分に残る限り刺客だけを下げられない（タグを最大4手動かしても
刺客8.33%、ボーナス3にすると他タイプが5.40〜13.53%に崩れ武士も2.15%に下がる）。
そこで**刺客のmobilityを削除**した（区分なし＝武士と同じくボーナス対象外）。
AXIS_BONUS=1・全タグ・全配点は据え置き、文面・点数の変更もない（`ninjaTypes.js`の
刺客から`mobility`を消し、`scoring.js`の開発時チェックに`NO_MOBILITY_TYPE_IDS`を
追加しただけ）。効果：刺客8.24→**6.20%**（武士3.66%、他10タイプ8.17〜10.66%。
実テスターの選択傾向で重み付けしても刺客約6.5%で順位は変わらない）。不採用にした案：
刺客を「定住」に変更（6.93%）、タグ4か所の変更（刺客6.02%だが、c7aの定住→時々
など文面に合わない変更を含むため）、案H（共通c3dのkamoku 2→1・庶民問1Cの間者+3。
不要になったため未実施）。

**追加調整その3（2026/9/20、ユーザーのExcel入力の反映）**：質問と点数の一覧(Excel)を
確認したユーザーが、共通7問の違和感を「こう直したい」という値で入力した。全て反映：
- 移動・定住区分：1A・4B・5D・7Aを「なし」に、1Cを転々、1Dを時々、5Bを時々、5Cを転々に。
- 社交性：3C・4C・6C・7Cを+1→+3、5Cを+1→0。
- 寡黙さ：3Bに+1、4Aに+2、4Dに+1を追加、3Dを+2→+1（4Bは+1のまま）。
- 2D「周囲をよく観察し、自分で判断できる材料を集める」：武家系+1を寺社系+1に置換
  （「追加」との比較で均等になる方を選択。武家系21.75%・寺社系17.53%・庶民60.72%に）。
- 1Bの文面：「心を落ち着け」→「心を落ち着かせ」（日本語のみ。翻訳表の日本語列も更新）。

入力を固定したまま庶民分岐などを再探索した。その際、ユーザー要望として「**常の形を12タイプ
中で最も高く**、**薬屋を庶民の中で最も低く**」を条件に加えた（刺客は「武士に次ぐレア」の
要望が先にあるため、薬屋は**刺客を除く庶民の中で最少**と解釈した）。再調整の内容：
常の形A=問1:4／問2:6／問3:4（旧3/3/3）、庶民問1B=商人2のみ、問1C=間者3・刺客2、
問1D=放下師2のみ、問2C=刺客3・間者2、問2D=猿楽師3のみ、問3Bの記述順=商人が先
（同点時）、武家系問3Aの武士+3。AXIS_BONUS=1・刺客の区分なしは据え置き。

**追加調整その4（2026/9/20、Excelの新しいメモの反映）**：ユーザーが再びExcelに入力した
メモ19件を反映した（ここまでで「常の形が最多・薬屋が刺客を除く庶民で最少・刺客が武士に
次ぐレア」の3条件を維持するよう再探索した。探索の許可は「猿楽師を武家系へ、虚無僧を
寺社系へ移動または追加することも検討してよい」だったが、点数の再調整だけで条件を満たせた
ため、分類のタイプ割り当ては変えていない）。
- 点数：共通1C・2Cの社交性+1→+2、3Aの武家系+2→+3、3Bの寡黙さ+1→+2、5Dの武家系+1を
  寺社系+1に置換（追加との比較で置換の方が均等）。武家系分岐は問1Dの虚無僧を0（薬師+2の
  みに）、問2D・問3Dに武士+1を追加。武家系問1Cの「虚無僧+2」はユーザーが「反映しない」
  と判断（薬師+2のまま）。
- 文面：寺社系9か所の「あからさますぎる」指摘（問1のA〜D、問2のA・C、問3のA・B・C）を
  日常の言い回しに変更（6章参照。英語版・翻訳表も更新）。庶民問1Aは「現地の人に紛れる」
  （英: Blend in with the locals.）に変更した。
- 再調整（庶民分岐など）：常の形A=問1:5／問2:5／問3:6、庶民問1Cは間者3のみ（刺客0）、
  武家系の武士A=問1:1／問2:1／問3:3。AXIS_BONUS・刺客の区分なし・その他は据え置き。
- 効果（等確率）：常の形11.46%（最多）／薬師10.35%／放下師9.58%／猿楽師9.34%／出家・山伏
  9.20%／虚無僧8.91%／商人8.64%／間者8.00%／薬屋7.59%（刺客を除く庶民で最少）／刺客
  4.49%（2番目にレア）／武士3.23%（最レア）。分類到達率は武家系22.49%／寺社系18.40%／
  庶民59.11%。刺客と武士の差は1.26ptとやや小さい。

**追加調整その5（2026/9/21、点数の上限を3にする）**：ユーザーが「点数は3ポイントを上限に
した方がよい」と判断し、「常の形の出現率は最高にならなくてもよい」と条件を緩めた。
庶民の各問のA（常の形+5／+5／+6）だけが3を超えていた。
- 検討結果：上限3を守ったまま、他の入力（社交性+3など）を変えずに調整すると、常の形は
  **約4%**まで下がり、刺客（5%）より少なくなった（社交性は商人・薬屋・放下師・猿楽師にだけ
  加算され、常の形には加算されないため。「刺客が2番目のレア」の条件も崩れる）。
- ユーザー判断：「常の形は寡黙と社交の両方の印象がある。両方バランスがよい、または両方加算
  する方が自然。社交性を+2にそろえる案もあり」。両方を全額加算すると常の形が13〜14%まで
  大きくなり上限3の範囲では抑えられないため、**shakouとkamokuの小さい方を加算する**
  「バランス型」を採用した（`scoring.js`の`resolveShominType`）。
- 変更：社交性c3c・c4c・c6c・c7cを+3→+2にそろえた（全C選択肢が+2、c5cは0）。常の形A=
  3／3／2。庶民問2Cは刺客3・間者1、問3Bは商人2・薬屋2、問3Cは刺客1・間者1。その他は据え置き。
- 効果（等確率）：薬師10.35%／放下師9.36%／間者9.31%／出家・山伏・商人9.20%／常の形8.96%／
  虚無僧8.91%／猿楽師8.79%／薬屋8.04%（刺客を除く庶民で最少）／刺客5.44%（2番目にレア）／
  武士3.23%（最レア）。分類到達率は変わらない（22.49／18.40／59.11%）。前の条件のうち
  「常の形が最多」だけを外し、他（薬屋が刺客を除く庶民で最少・刺客が武士に次ぐレア）は維持。

### 出現率（全パターン総当たりの実測値・平等化調整＋追加調整その1〜その5後、2026/9/21）

共通7問は4^7=16,384通り、分岐後は3分類とも4^3=64通り。
**各共通7問の組み合わせを均等に1/16,384と重み付けし、その内側で分岐後の
組み合わせを均等に1/64と重み付けする**、2段階の期待値計算で実測している。
実装済みの`questions.js`/`scoring.js`/`ninjaTypes.js`そのものを使って総当たりした値が、
探索用シミュレーターの値と一致することを確認している。

| タイプ | 出現率 | | タイプ | 出現率 |
|---|---|---|---|---|
| 薬師 | 10.35% | | 常の形 | 8.96% |
| 放下師 | 9.36% | | 虚無僧 | 8.91% |
| 間者 | 9.31% | | 猿楽師 | 8.79% |
| 出家 | 9.20% | | 薬屋 | 8.04%（刺客を除く庶民で最少） |
| 山伏 | 9.20% | | **刺客** | **5.44%（2番目にレア）** |
| 商人 | 9.20% | | **武士** | **3.23%（最レア）** |

分類の内部到達率：武家系22.49%／寺社系18.40%／庶民59.11%（2D・5Dの置換で寺社系が
やや増えた。それ以前は23.41／16.33／60.25）。武士・刺客以外の10タイプは8.04〜10.35%に
収まった。
12タイプ全て到達可能／武士が最レア／刺客が2番目にレア／薬屋が（刺客を除く）庶民で
最少／全点数が3以下／合計100%を確認済み。

**旧値（履歴）**：
- v3.1（〜2026/9/19）：虚無僧21.89%／薬師16.42%／出家・山伏11.21%／商人9.56%／
  放下師9.31%／常の形6.31%／薬屋4.15%／猿楽師4.14%／間者3.99%／刺客1.20%／
  武士0.61%。分類到達率は武家系38.92%／寺社系22.41%／庶民38.67%（旧版のこの節は
  39.08%・22.25%と書いていたが実測は38.92%・22.41%だった）。
- Excelメモ反映後・上限3ポイント適用前（9/20 4回目）：常の形11.46%／薬師10.35%／放下師9.58%／
  猿楽師9.34%／出家・山伏9.20%／虚無僧8.91%／商人8.64%／間者8.00%／薬屋7.59%／刺客4.49%／
  武士3.23%。分類到達率は22.49／18.40／59.11%。
- Excel入力の反映後（9/20 3回目・追加調整その3まで）：常の形10.77%／虚無僧9.84%／猿楽師9.76%／
  放下師9.51%／出家・山伏8.76%／間者8.72%／商人8.38%／薬師8.14%／薬屋7.91%／刺客5.68%／
  武士3.76%。分類到達率は21.75／17.53／60.72%。
- 均等化＋c4b調整＋刺客の区分なし（9/20 2回目。Excel入力の反映前）：薬師10.66%／薬屋9.68%／
  常の形9.47%／間者9.22%／虚無僧9.09%／放下師8.99%／猿楽師8.40%／商人8.29%／
  出家・山伏8.17%／刺客6.20%／武士3.66%。分類到達率は23.41／16.33／60.25%。
- 案A＋軸ボーナス2（2026/9/20の1回目の反映）：薬師12.15%／放下師12.14%／商人11.87%／
  虚無僧10.89%／常の形10.68%／薬屋9.81%／山伏9.09%／出家7.25%／間者6.80%／
  猿楽師6.69%／刺客2.25%／武士0.37%。案Aのみ（軸なし）だと商人13.40%・虚無僧13.17%・
  放下師13.15%と上位が13%を超えていた。

**経緯**：テスターの「3人中3人が虚無僧」報告を受けて調べたところ、v3.1は理論値でも
虚無僧が21.89%で突出していた（共通7問の段階で約39%が武家系に進み、武士はほぼ
出ないため虚無僧・薬師の2タイプで山分けする構造）。そこで①案A（共通Dの庶民ポイント
を+1→+2）と②移動・定住軸ボーナスを導入し、さらに上記の平等化調整を行った。

**注意（実データとの比較）**: 上表は「全員が各選択肢を等確率で選ぶ」前提の理論値。
2026/9/20時点の実テスター22件（ユニーク16セッション、旧ロジック）の選択傾向
（共通7問のA/B/C/D回数：c1=3/4/9/6、c2=7/4/9/2、c3=4/4/4/10、c4=7/2/11/2、
c5=9/0/9/4、c6=3/9/5/5、c7=2/5/8/7。寺社系のB選択肢が少なく、C・Dが多い）で
重み付けし直すと、新ロジックでは庶民到達が約75%となり、庶民は商人13.4%・放下師12.9%・
猿楽師12.4%・薬屋11.4%・常の形10.2%・間者9.6%・刺客5.5%、出家・山伏は各約4.3%、虚無僧
6.2%、薬師7.7%、武士2.3%になる見込み（n=22のため
有意ではない参考値。寺社系が少ないのは、人がB（心を落ち着ける等）を選びにくい
実際の傾向によるもので、質問文を変えない限り配点では補正しきれない）。
デプロイ後の実データで再比較し、偏りが出れば追加調整を検討すること。

質問文・選択肢・タイブレーク順を変更するたびに、必ず総当たりを回して
「出現しないタイプがないか」「武士が全タイプ中もっともレアなままか」
「特定のタイプに極端に集約されていないか」を再確認すること。

## 8. アセット命名規則

`src/assets/characters/`配下に、5章の`id`列に対応する形で配置：

```
komuso.png
shukke.png
yamabushi.png
akindo.png
hokashi.png
sarugakushi.png
tsunenokatachi.png
shikaku.png
kanja.png
bushi.png
kusuriya.png
kusushi.png
```

イラストは別途Firefly / Nano Banana 2で生成済み（依頼書：
「忍者タイプ診断_イラスト指示書_Firefly_NanoBanana2版.md」参照）。
すべて縦長3:4、和紙風カード枠＋毛筆タイトル文字＋落款印入りの完成品を想定。

---

## 9. 実装の優先順位（MVP → 拡張）

**MVP（まずここまで）**
- [ ] StartScreen → QuestionScreen（10問）→ ResultCard の一直線フロー
- [ ] スコアリングロジックの実装
- [ ] 12タイプ分のイラスト差し込み
- [ ] レスポンシブ対応（スマホ優先）

**拡張（余力があれば）**
- [ ] 結果カードのSNSシェア（画像として書き出し／OGP対応）
- [ ] 診断結果の相性表示（自分のタイプと相性のいい/悪いタイプ）
- [ ] リトライ導線（もう一度診断する）
- [ ] 結果カードのダウンロード機能（`html2canvas`等）

---

## 10. 未確定事項（要相談）

- 質問の点数バランスは初期ドラフトのため、実装後の調整が前提
- SNSシェア機能を実装するかどうかは未定

## 11. 英語版(i18n)対応

「忍者タイプ診断アプリ:英語版仕様書」に基づき実装。ルートの`CLAUDE.md`が定める
「6つの埋め込みアプリを個別に(専用のClaude Codeで)先に翻訳」という方針に沿った
対応（このアプリ単体の翻訳）。

### 実装方式
- 診断ロジック(`scoring.js`)は言語に依存しないため変更なし。
- `App.jsx`に`lang`state('ja'|'en'、既定'ja')を追加。右上固定の言語切替ボタン
  (`.lang-toggle`)は**開始画面(`phase === 'start'`)でのみ表示**する。診断中に
  言語が切り替わると設問の並び順や進行が分かりにくくなるため、開始前に選び
  切ってもらう方針(「もう一度診断する」で開始画面に戻れば再度切り替え可能。
  1回の診断中は言語固定)。`StartScreen`/`QuestionScreen`/`ResultCard`(経由で
  `TypeGallery`)に`lang`をpropsで渡すシンプルなprop drilling方式(状態は
  localStorage等に永続化しない)。
- `ninjaTypes.js`の各タイプに`epithetEn`(英語の添え名、例: "The Assassin")・
  `keywordEn`・`descriptionEn`を追加。既存の`nameEn`(ローマ字、例: "Shikaku")を
  英語モードでの主表示名としてそのまま流用。日本語版の`name`/`keyword`/
  `description`は一切変更していない(標準instruction厳守)。
  ※`tsunenokatachi`のみ`nameEn`/`epithetEn`をそれぞれ"Tsunenokatachi"/
  "The Ordinary Form"から"Tsunenokata"/"The Peasant"に変更(ユーザー指定)。
- 英語版の固有名詞(`nameEn`)＋添え名(`epithetEn`)は、結果画面
  (`ResultCard.jsx`)・十二タイプ一覧(`TypeGallery.jsx`)のどちらも
  **2行表示**にしている(例: 1行目"Komuso"、2行目"The Hermit Monk")。
  日本語版は「虚無僧 KOMUSO」のように短いローマ字コードを横並びで
  小さく添えるだけで済むが、英語版の添え名は"The Hermit Monk"のような
  文の一部なので、同じ横並び・大文字化・広い字間のスタイルのままだと
  読みにくい。`isEn`の時だけ専用の修飾クラス
  (`.result-card__title-en--epithet` / `.gallery__name--stacked`)を付け、
  大文字化・字間を解除してイタリック体で2行目に自然に収まるようにしている。
- `questions.js`の各質問・各選択肢に`textEn`を追加(内部スコア・id・日本語の
  `text`は変更なし)。
- 結果画面の落款印(`result-card__seal`)は、和柄の印章という性質上、英語版でも
  日本語名の先頭2文字のまま(意図的に翻訳しない。名刺ロゴのような装飾要素という
  扱い)。
- `TitlePlate`(`decorations.jsx`)に`horizontal`propを追加。日本語版の縦書き
  (`writing-mode: vertical-rl`)はラテン文字だと読みにくいため、英語版の
  タイトルプレート("NINJA TYPE QUIZ")だけ横書き表示に切り替える
  (`.title-plate--horizontal`)。落款印(`seal`)自体は言語を問わず共通。
- **英語版の質問文・選択肢文には、意味の区切りで改行(`\n`)を埋め込んでいる**
  （`QuestionScreen.jsx`の`withLineBreaks()`が`\n`を`<br />`に変換して描画。
  日本語版の`text`には`\n`を含めていないため、日本語表示には影響しない）。
  基準: ①2文(ピリオドで区切られた「状況説明。質問？」の形)は必ずピリオドの
  後で改行する（例:「You've been given a new task at work.」／「What do you
  do first?」）。②1文中のカンマは、カンマの前が"First,"のような短い前置き
  （1〜3語程度の副詞・条件節・前置詞句）の場合は改行しない（例:「First, look
  into records and past cases to find a reliable approach.」はそのまま）。
  カンマの前が動詞を伴うある程度まとまった独立節（目安4語以上、または短くても
  動詞+目的語で完結する節）の場合はカンマの後で改行する（例:「Value courtesy,」
  ／「earning trust through knowledge and sincerity.」）。この基準は
  `questions.js`のtextEnにのみ適用し、`ninjaTypes.js`のdescriptionEn等
  （地の文として自然に折り返される想定）には適用していない。

### 注意点
- 内部分類(武家系/buke・寺社系/jisha・庶民/shomin)は英語版でも画面に一切
  表示しない(1章の方針を英語版でも継続)。英訳作業でも分類の意味を説明する
  ような言い回しは避けている。
- 翻訳表(全UI文言・12タイプ・全質問・全選択肢の日英対訳)を
  `翻訳表_忍者タイプ診断アプリ.xlsx`としてリポジトリ直下に出力済み
  (6シート: UIストリング/12タイプ/共通7問/分岐_武家系/分岐_寺社系/分岐_庶民)。
  2026/9/20に、日本語の口調変更（1章）に合わせて該当行の日本語列を更新し、
  常の形の英語名を`Tsunenokata`/`The Peasant`に揃えた。

## 12. セキュリティレビュー記録

「security-check-ninja-diagnosis.md」の指示書に基づき実施(2026年9月)。
結果はすべて「問題なし」:

- `gitleaks detect`: リーク検出なし
- `npm audit`: 0件(critical/high/moderate/low/info すべて0)
- **質問文・選択肢の文面にも内部分類名が出ていないかを確認すること**: 2026/9/20に、庶民
  問1Aの旧文面「一般庶民になりきって、紛れ込む」に「庶民」が出ていたことをユーザーの
  指摘で発見し、「現地の人に紛れる」に直した（`questions.js`の`text`/`textEn`を
  `武家系|寺社系|庶民`でgrepすれば検出できる。「寺社」だけの語は分類名ではないが、
  紛らわしいので寺社系分岐の文面は日常語に寄せた）。
- 内部分類名(buke/jisha/shomin/武家系/寺社系/庶民)の漏洩チェック(最重要項目):
  静的解析(JSXの描画箇所に一切出現しないこと)・動的検証(実際に診断を完走し、
  ページテキスト・DOM属性・console・localStorage/sessionStorage・URLのいずれにも
  現れないこと)の両方で確認。英語版追加によって新たな漏洩経路も生じていない
- 「戻る」ボタンのロジック(`deriveState`ベースの導出設計)を全分岐パターンで
  再確認し、古い分類/結果が残らないことを確認
- `prefers-reduced-motion`・キーボード操作性(全ボタンが実`<button>`要素)を確認

**バランス面の副次的な発見(セキュリティとは別軸)**: レビュー時点の総当たり検証で
庶民の薬屋(1.14%)・刺客(0.33%)が武士(1.4474%)より低い出現率になっていたが、
**7章で解消済み**（v3.1で庶民系問3を均等配点化し、2026/9/20の案A＋軸ボーナスと平等化調整で
薬屋8.04%・刺客5.44%となり、武士3.23%が最レア、刺客が2番目のレアになった）。

## 13. 診断結果の匿名データ収集(`src/lib/analytics.js`)

**経緯**: 実際にテスターに試してもらったところ「3人中3人が虚無僧になった」
との報告があり、原因(総当たり計算上の偏り／実際の回答分布の偏り／実装の
不具合、のどれなのか)を調査するため、リリース済みの本番環境でも実際の
回答パターンを収集できるようにした。ブラウザでの総当たり計算だけでは
「実在する人間が実際にどう答えるか」までは分からないため。

### 収集する内容(個人情報は一切含めない)

- `lang`: 診断時の言語('ja'/'en')
- `common_picks`: 共通7問で選んだ選択肢idの配列(例: `["c1a","c2c",...]`)
- `branch_picks`: 分岐後3問で選んだ選択肢idの配列
- `faction_id`: 内部分類(buke/jisha/shomin)。ユーザーには見せない値だが、
  調査目的では有用なため収集する
- `result_id`: 最終タイプid(例: `"komuso"`)
- `client_session_id`: ブラウザのlocalStorageにランダム生成して保存するUUID
  (個人情報ではない。同じ人の複数回診断を後から見分けるためだけの用途)

名前・メールアドレス・IPアドレス・ユーザーエージェント等は一切送信しない。

### 保存先とその設計

- 保存先はSupabaseの`ninja-training-log`プロジェクト
  (`rtecihjilnuhhgtpogdw`)内の`public.ninja_shindan_diagnosis_events`
  テーブル。**このアプリ(NinjaJobsリポジトリ)専用の無関係な独立テーブル**
  であり、`ninja-training-log`アプリ自身のテーブル(`training_logs`/
  `user_progress`)とはスキーマ・外部キーともに一切関連しない。
  新規にSupabaseプロジェクトを作ると無料枠のactiveプロジェクト数上限に
  近づくため、既存の空いている無料枠を間借りする形にした(ユーザー承認済み)。
- RLSは「anonキーからのINSERTのみ許可」で、SELECT/UPDATE/DELETEのポリシーは
  一切定義していない。つまり公開されている`.env`のanonキー経由では
  **書き込みだけができ、読み取ることはできない**(読み取りはSupabase
  ダッシュボードのSQL Editor等、プロジェクト所有者の権限でのみ可能)。
- `@supabase/supabase-js`は1件INSERTするだけの用途には過大(バンドルサイズが
  約230KB増える)なため使わず、PostgRESTのREST APIに`fetch`で直接POSTする
  実装にした(このアプリの「バックエンドなし・依存最小限」という方針を保つ
  ため)。
- `.env`(`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`)は`.gitignore`済みで
  リポジトリには含まれない。値は`.env.example`を参照。

### 実装

- `App.jsx`で結果画面(`phase === 'result'`)に到達した瞬間に1回だけ
  `logDiagnosisEvent()`を呼ぶ(`useRef`で同一ラウンド内の二重送信を防止、
  「もう一度診断する」でリセットして次の診断では再度送れるようにする)。
- 送信は完全にfire-and-forget。失敗しても`console.warn`するだけで診断結果の
  表示には一切影響しない(通信環境が悪い訪問者の体験を損なわないため)。

### データの見方

Supabase側で`select common_picks, branch_picks, faction_id, result_id, lang
from ninja_shindan_diagnosis_events order by created_at desc`のように問い合わせ、
実際の回答パターンと結果の対応を確認する。十分な件数が集まった時点で、
7章の総当たり計算値(理論値)と実際の分布を比較し、偏りが理論通りの範囲内か
(単に少人数でのサンプル差)、それとも実装の不具合か再度判断すること。

**旧ロジックのデータの扱い（2026/9/20）**: 2026/9/20より前に集まった22件
（ユニーク16セッション）は旧ロジック(v3.1)の結果。同日に複数回反映しており
（1回目=案A＋軸ボーナス2、2回目=平等化調整＋c4bの微調整＋刺客の区分なし、
3回目=Excel入力の反映＋庶民分岐の再調整、4回目=Excelの新しいメモ19件の反映＋文面変更、
5回目(9/21)=点数の上限3の適用＋常の形の加算方式の変更。7章参照）、現行ロジックはこの5回目以降。理論値との比較は`created_at`で新旧を分けて行うこと。収集済みの
`common_picks`/`branch_picks`（選択肢id）から、旧・新どちらのロジックの結果も
後から再計算できる（`axisWinner`や`logic_version`用のカラムは追加していない）。
新ロジック(2026/9/20〜)に切り替わった後のテスト診断で本番DBへ行が増えないよう、
ブラウザ確認では`window.fetch`を無効化するか、確認後に自分の
`client_session_id`の行だけを削除すること（`.env`入りのビルドは本番DBへ送信する）。

## 14. 確認用Excel一覧(質問とポイント)— 変更のたびに更新して見せる

ユーザーの要望（2026/9/20）：**質問文・点数・移動定住の区分を変更するたびに、確認用の
Excel一覧を最新の内容に更新してから報告する**（違和感がないかを人が確認するため）。

- 生成：`npm i --no-save exceljs && node scripts/build-review-xlsx.mjs`
  （exceljsは実行時だけ入れる。`scripts/optimize-characters.mjs`と同じ流儀で、アプリの
  依存には入れない）。出力はプロジェクト直下の`忍者診断_質問とポイント一覧.xlsx`
  （git管理外。データは`questions.js`・`ninjaTypes.js`・`scoring.js`から直接作るので
  実装と一致する）。
- シート：1_仕組みの説明／2_共通7問（分類点・社交性・寡黙さ・移動定住区分）／
  3〜5_分岐（武家系・寺社系・庶民）／6_職業と区分（現在の出現率）／7_出現率の推移。
  各シート右端に「違和感(○/△/×)」「確認メモ」欄がある。ユーザーはここに、気になる点や
  「こう直したい」という値を書き込む。
- ユーザーの入力が入ったファイルを上書きしないよう、既存ファイルに入力があれば
  `…_入力済み_日時.xlsx`として別名で残してから再生成する。**入力済みのファイルは
  プロジェクトフォルダ以外（`C:\dev\Homepage\`直下）に保存されていたことがある**
  （2026/9/20）ため、入力を読むときは同名ファイルを検索して、更新日時の新しいものを確認する。
- 出現率の推移（7シート）：ロジックを変える**直前**に
  `node scripts/build-review-xlsx.mjs --snapshot "ラベル"`で、その時点の値を
  `scripts/review-xlsx-history.json`に履歴として残す。
- 作業の順序：①変更前にsnapshot ②変更・総当たり検証・ブラウザ確認 ③Excel再生成
  ④CLAUDE.md更新 ⑤報告（更新したExcelの場所も伝える）。点数や区分を動かす提案は、
  対象の質問文・選択肢の全文と点数を並べて見せ、承認を得てから実装する。
