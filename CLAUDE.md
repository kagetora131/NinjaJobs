# 忍者タイプ診断アプリ — CLAUDE.md

このドキュメントはClaude Codeがこのプロジェクトを実装する際の指示書です。
「職業診断」形式の忍者版で、10問の質問に答えると12種類の忍者タイプのうち
どれに当てはまるかがカード形式で表示されるWebアプリを作ります。

## 1. プロジェクト概要

- **タイトル（仮）**：忍者タイプ診断
- **コンセプト**：一般的な「職業適性診断」の忍者版。共通7問で「武家系/寺社系/庶民」の
  3分類を内部判定し（ユーザーには一切見せない）、分類確定後の3問で最終タイプ(七方出
  由来の7タイプ＋刺客・間者・武士・薬屋・薬師の計12タイプ)を決める(計10問構成、詳細は
  `ninja-shindan-question-design` skill)。
- **トーン＆マナー**：和風・時代劇風、中二病的な「らしさ」を楽しめる演出(例：「忍びの
  道は――**刺客**」)。**「お前」・です/ます調は使わない**(常体。2026/9/20テスター指摘で
  変更。英語版は変更なし)。
- **ホスティング**：無料枠（Vercel / Netlify等）。バックエンド不要、全てクライアント
  サイドで完結させる。

## 2. 技術スタック

- **フレームワーク**：React + Vite。**状態管理**：`useState`/`useReducer`で十分。
- **スタイリング**：CSS Modules(Tailwindは使わず、和風の世界観をトークンベースの
  独自CSSで作り込む方針。詳細は4章)。
- **アニメーション**：CSSトランジション＋演出の質を上げたい箇所に限り`Framer Motion`。
- **バックエンド**：なし。診断ロジック・結果表示はすべてフロントエンドで完結。
  **デプロイ**：Vercel想定(`vite build`の成果物をそのままデプロイ)。

## 3. ディレクトリ構成（提案）

```
ninja-shindan/
├── CLAUDE.md
├── README.md
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── data/
│   │   ├── ninjaTypes.js       # 12タイプの定義（5章）
│   │   ├── factions.js         # 3分類(武家系/寺社系/庶民)の定義（内部専用）
│   │   └── questions.js        # 共通7問 + 分類別の分岐後3問（question-design skill）
│   ├── components/
│   │   ├── StartScreen.jsx        # タイトル・診断開始画面
│   │   ├── QuestionScreen.jsx     # 全10問共通の質問表示・選択肢UI
│   │   ├── ProgressBar.jsx        # 「第◯問」の進捗表示
│   │   ├── ResultCard.jsx         # 診断結果カード（掛軸仕立て）
│   │   ├── TypeGallery.jsx        # 結果画面下の十二タイプ一覧（サムネイル＋説明ダイアログ。分類分けは見せない）
│   │   └── decorations.jsx        # 手裏剣・題字プレート等の装飾パーツ
│   ├── logic/
│   │   └── scoring.js          # 分類判定＋最終タイプ判定（question-design skill）
│   ├── styles/
│   │   ├── tokens.css          # デザイントークン（4章）
│   │   └── global.css
│   └── assets/
│       └── characters/         # 12タイプのイラスト（8章の命名規則で配置）
```

## 4. デザイン方針

このアプリ固有の「和風・時代劇画×モダンアニメ」の世界観を、AIにありがちな
テンプレ配色（生成りベース＋テラコッタ／黒背景＋ネオン一色）に寄せず、
イラストの世界観（藍・墨・紅・金）から直接トークンを起こす。

### カラートークン
```css
:root {
  --color-sumi:        #1A1A22; /* 墨・base背景（濃紺寄りの黒） */
  --color-washi:       #EDE6D6; /* 生成り・カード背景、和紙 */
  --color-beni:        #B23A2E; /* 紅・落款印、アクセント（CTAボタン等） */
  --color-kondo:       #C9A227; /* 金泥・タイトルの縁取り、演出の輝き */
  --color-ainezu:      #3E4A5C; /* 藍鼠・サブテキスト、枠線 */
  --color-text-light:  #F4F0E6; /* 濃色背景上の文字 */
  --color-text-dark:   #221F1A; /* 淡色背景上の文字 */
}
```
タイプごとの識別色は各イラストの背景色をそのまま結果カードのアクセントカラーとして
流用する。

### タイポグラフィ・レイアウト・アクセシビリティ
- 見出し・タイプ名は明朝体の毛筆寄りフォント(`Shippori Mincho`等)、本文・質問文は
  ゴシック体(`Noto Sans JP`等)。ともにGoogle Fontsで無料利用可能。
- 1画面1質問のシングルカラム構成(スマホ優先)。**結果画面が最大の見せ場**
  (暗転→和紙→イラスト→毛筆タイトルのシグネチャー演出。結果カードの落款印はイラストと
  重複するため9/24に削除)、装飾は結果画面に集中させ質問画面はシンプルに保つ。
- 9/24のUI改善(選んだ札を光らせてから次へ進む／タッチ端末のhover残り対策／文節改行／
  十二タイプ一覧のサムネイル化)と、「戻る」ボタン・選択肢シャッフルのstate設計等の
  詳細な理由は`docs/実装ノート.md`を参照。
- キーボード操作(tab/enter)対応、`prefers-reduced-motion`尊重、WCAG AA相当の
  コントラスト比を目安にする。

## 5. 12タイプ定義（`src/data/ninjaTypes.js`のもと）

| id | タイプ名 | キーワード | 性格傾向 | 移動・定住(mobility、内部専用) |
|---|---|---|---|---|
| komuso | 虚無僧 | 隠遁・静寂・内省 | 一人の時間を好み、感情を表に出さず物事を静かに観察する | hyohaku(転々) |
| shukke | 出家 | 悟り・無欲・奉仕 | 執着を捨て、人のために動くことに喜びを感じる | teiju(定住) |
| yamabushi | 山伏 | 修行・忍耐・自然 | 肉体と精神を鍛えることに価値を置き、困難を厭わない | hyohaku(転々) |
| akindo | 商人 | 社交・情報通・損得 | 人付き合いが得意で、交渉や駆け引きを楽しむ | tokidoki(時々) |
| hokashi | 放下師 | 芸・機転・身軽さ | 目立つことを恐れず、場を盛り上げるのが得意 | hyohaku(転々) |
| sarugakushi | 猿楽師 | 二面性・演技・仮面 | 状況に応じて自分を演じ分けるのが得意 | tokidoki(時々) |
| tsunenokatachi | 常の形 | 擬態・堅実・目立たなさ | 普段は目立たず、いざという時に力を発揮する | teiju(定住) |
| shikaku | 刺客 | 冷徹・単独・目的遂行 | 感情より目的を優先し、迷わず行動する | **なし(ボーナス対象外。2026/9/20に「時々」から外した)** |
| kanja | 間者 | 情報収集・洞察・裏工作 | 物事の裏側を読み取り、慎重に立ち回る | tokidoki(時々) |
| bushi | 武士 | 正義・名誉・正々堂々 | 正面から向き合うことを重んじ、筋を通す | **なし(ボーナス対象外)** |
| kusuriya | 薬屋 | 実利・商才・駆け引き | リスクとリターンを見極め、実利を取りに行く | hyohaku(転々) |
| kusushi | 薬師 | 冷静・分析・専門知識 | 状況を分析し、知識で周囲を助ける役に回る | teiju(定住) |

各タイプには結果画面用に以下の情報も持たせる：
- `name`（表示名）／`nameEn`（ローマ字、ファイル名等に使用）
- `description`（診断結果として表示する2〜3文の説明文。作成済み・変更しない）
- `accentColor`（4章のイラスト背景色に対応する16進カラー）
- `imagePath`（8章の命名規則に対応する画像パス）
- `mobility`（内部専用。上表の右端列。'teiju'/'tokidoki'/'hyohaku'。question-design
  skillの「移動・定住軸」のボーナス判定にだけ使い、画面・DOM・console・storage・URLには
  出さない。**武士と刺客は意図的に持たない**）

## 6. 質問設計・診断ロジック(要点)

質問文・配点・タイブレーク・出現率実測値などの詳細は`ninja-shindan-question-design`
skill(`.claude/skills/ninja-shindan-question-design/SKILL.md`)に移設した
(実装履歴として全文保存、質問/配点を触る作業では必ず読むこと)。要点のみここに残す:

- 共通7問(`COMMON_QUESTIONS`)で「武家系(buke)／寺社系(jisha)／庶民(shomin)」の
  3分類を内部判定(**ユーザーには一切見せない**)、分類確定後の3問(`BUKE_QUESTIONS`/
  `JISHA_QUESTIONS`/`SHOMIN_QUESTIONS`)で12タイプのうち1つを最終決定する。
- 武士だけ低配点＋「同点勝ち不可」の特別ルールで自然にレア化(`resolveBukeType`)。
  刺客は移動・定住軸のボーナス対象外にして「武士に次ぐレア」に調整済み。
- 点数の上限は3ポイント(全選択肢のスコアは3以下を維持すること)。
- 質問文・選択肢・タイブレーク順を変更するたびに、必ず総当たり(`scoring.js`の
  ロジックを再現するシミュレーター)を回し、出現しないタイプがないか、武士が
  最もレアなままか、特定タイプへの偏りがないかを再確認する。
- 実装ファイル: `src/data/questions.js`(質問文・配点)、`src/data/factions.js`
  (3分類とタイプの割り当て)、`src/logic/scoring.js`(判定ロジック本体)。

## 8. アセット命名規則

`src/assets/characters/`配下に、5章の`id`列の値+`.png`(例:`komuso.png`)で12タイプ分
配置する。イラストは別途Firefly / Nano Banana 2で生成済み(依頼書:
「忍者タイプ診断_イラスト指示書_Firefly_NanoBanana2版.md」参照)。すべて縦長3:4、
和紙風カード枠＋毛筆タイトル文字＋落款印入りの完成品を想定。

## 9. 実装の優先順位（MVP → 拡張）

**MVP（まずここまで）**
- [ ] StartScreen → QuestionScreen（10問）→ ResultCard の一直線フロー
- [ ] スコアリングロジックの実装
- [ ] 12タイプ分のイラスト差し込み
- [ ] レスポンシブ対応（スマホ優先）

**拡張（余力があれば）**
- [ ] 結果カードのSNSシェア（画像として書き出し／OGP対応）
- [ ] 診断結果の相性表示（自分のタイプと相性のいい/悪いタイプ）
- [ ] リトライ導線（もう一度診断する）
- [ ] 結果カードのダウンロード機能（`html2canvas`等）

## 10. 未確定事項（要相談）

- 質問の点数バランスは初期ドラフトのため、実装後の調整が前提
- SNSシェア機能を実装するかどうかは未定

## 11. 英語版(i18n)対応

「忍者タイプ診断アプリ:英語版仕様書」に基づき実装(6つの埋め込みアプリを個別に
先に翻訳、という方針に沿った対応)。診断ロジックは言語に依存せず変更なし。`App.jsx`の
`lang`state(開始画面でのみ切替可、診断中は固定)を各コンポーネントにprops経由で渡す。
`questions.js`/`ninjaTypes.js`に`textEn`/`nameEn`/`epithetEn`/`descriptionEn`を追加し、
日本語側の値は一切変更していない。内部分類名(buke/jisha/shomin)は英語版でも非表示を
継続。実装の詳細(2行表示・改行ルール等)は`docs/実装ノート.md`を参照。
翻訳表は`翻訳表_忍者タイプ診断アプリ.xlsx`(リポジトリ直下、6シート)。

## 12. セキュリティレビュー記録

「security-check-ninja-diagnosis.md」に基づき実施(2026年9月)、結果はすべて問題なし
(gitleaks・npm audit・内部分類名の漏洩チェック・戻るボタンの状態管理・
アクセシビリティ)。詳細は`docs/実装ノート.md`を参照。

## 13. 診断結果の匿名データ収集(`src/lib/analytics.js`)

実際の回答分布を検証するため、本番環境でも匿名の回答パターン(選択した選択肢id・
内部分類・結果タイプ・言語・ランダムなセッションUUID)をSupabase
(`ninja-training-log`プロジェクトの専用テーブル、anonキーはINSERTのみ許可)に
収集している。氏名・メール・IP・UAは送信しない。保存先の設計・実装・データの見方の
詳細は`docs/実装ノート.md`を参照。

## 14. 確認用Excel一覧(質問とポイント)

質問文・点数・移動定住の区分を変更するたびに、確認用Excel一覧を再生成してユーザーに
見せる運用ルールがある。手順は`ninja-shindan-review-excel` skillを参照
(このアプリの質問/配点を変更する作業では必ず読むこと)。

