# 忍者タイプ診断アプリ — CLAUDE.md

このドキュメントはClaude Codeがこのプロジェクトを実装する際の指示書です。
「職業診断」形式の忍者版で、10問の質問に答えると12種類の忍者タイプのうち
どれに当てはまるかがカード形式で表示されるWebアプリを作ります。

---

## 1. プロジェクト概要

- **タイトル（仮）**：忍者タイプ診断
- **コンセプト**：一般的な「職業適性診断」の忍者版。共通7問＋系統別3問の
  計10問（二択〜四択）に答えると、七方出（しちほうで）由来の7タイプ＋
  刺客・間者・武士・薬屋・薬師の計12タイプのうち、系統を絞ってから
  系統内で確定したタイプが「診断結果カード」として表示される（6章）。
  選択と結果が矛盾するパターンを防ぐ「除外フラグ」も一部の選択肢に設定できる（7章）
- **トーン＆マナー**：和風・時代劇風。ふざけすぎず、少し中二病的な「らしさ」を楽しめる
  雰囲気（例：「お前は～の忍者だ」ではなく「お前の忍びの道は――**刺客**」のような演出）
- **ホスティング**：無料枠（Vercel / Netlify等）を前提。バックエンド不要、
  全てクライアントサイドで完結させる

---

## 2. 技術スタック

- **フレームワーク**：React + Vite
- **スタイリング**：CSS Modules または通常のCSS（Tailwindは使わず、和風の世界観を
  トークンベースの独自CSSで作り込む方針。詳細は4章）
- **状態管理**：React標準の`useState`/`useReducer`で十分（Redux等は不要な規模）
- **アニメーション**：CSSトランジション＋必要に応じて`Framer Motion`
  （結果カードのめくり演出など、演出の質を上げたい箇所に限定して使用）
- **画像アセット**：`/src/assets/characters/`配下にタイプごとのイラストを配置
- **バックエンド**：なし。診断ロジック・結果表示はすべてフロントエンドで完結
- **デプロイ**：Vercel想定（`vite build`の成果物をそのままデプロイ）

---

## 3. ディレクトリ構成（提案）

```
ninja-shindan/
├── CLAUDE.md
├── README.md
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── data/
│   │   ├── ninjaTypes.js       # 12タイプの定義（5章）
│   │   ├── systems.js          # 5系統の定義と所属タイプ（6章）
│   │   └── questions.js        # 共通7問 + 系統別3問（6章）
│   ├── components/
│   │   ├── StartScreen.jsx        # タイトル・診断開始画面
│   │   ├── QuestionScreen.jsx     # 共通問の表示・選択肢UI
│   │   ├── BranchQuestionScreen.jsx # 系統別問（系統名の帯つき）
│   │   ├── ProgressBar.jsx        # 「第◯問」の進捗表示
│   │   ├── ResultCard.jsx         # 診断結果カード（掛軸仕立て）
│   │   ├── TypeGallery.jsx        # 結果画面下の十二タイプ一覧（系統別）
│   │   └── decorations.jsx        # 手裏剣・題字プレート等の装飾パーツ
│   ├── logic/
│   │   └── scoring.js          # 系統判定＋系統内タイプ判定（7章）
│   ├── styles/
│   │   ├── tokens.css          # デザイントークン（4章）
│   │   └── global.css
│   └── assets/
│       └── characters/         # 12タイプのイラスト（8章の命名規則で配置）
```

---

## 4. デザイン方針

このアプリ固有の「和風・時代劇画×モダンアニメ」の世界観を、AIにありがちな
テンプレ配色（生成りベース＋テラコッタ／黒背景＋ネオン一色）に寄せず、
イラストの世界観（藍・墨・紅・金）から直接トークンを起こす。

### カラートークン
```css
:root {
  --color-sumi:        #1A1A22; /* 墨・base背景（濃紺寄りの黒） */
  --color-washi:       #EDE6D6; /* 生成り・カード背景、和紙 */
  --color-beni:        #B23A2E; /* 紅・落款印、アクセント（CTAボタン等） */
  --color-kondo:       #C9A227; /* 金泥・タイトルの縁取り、演出の輝き */
  --color-ainezu:      #3E4A5C; /* 藍鼠・サブテキスト、枠線 */
  --color-text-light:  #F4F0E6; /* 濃色背景上の文字 */
  --color-text-dark:   #221F1A; /* 淡色背景上の文字 */
}
```
タイプごとの識別色は各イラストの背景色（虚無僧＝青灰、出家＝琥珀、山伏＝山緑…）
をそのまま結果カードのアクセントカラーとして流用し、統一感を持たせる。

### タイポグラフィ
- **見出し・タイプ名**：明朝体の毛筆寄りフォント（例：`Shippori Mincho`
  または`Zen Old Mincho`）。診断結果のタイプ名はこのフォントを大きく使い、
  「筆文字が浮かび上がる」演出の主役にする
- **本文・質問文**：可読性重視のゴシック体（例：`Zen Kaku Gothic New`
  または`Noto Sans JP`）
- どちらもGoogle Fontsで無料利用可能

### レイアウト・演出方針
- 1画面1質問のシングルカラム構成。スマホでの利用を主軸に設計
- 質問→回答のたびに軽いフェード/スライド遷移
- **結果画面が最大の見せ場**：暗転→和紙が広がるような背景→イラストがフェードイン
  →タイトル文字が毛筆で書かれるように現れる→最後に落款印が「ポン」と押される
  演出（Framer Motionのタイミング制御で実現）。これが本アプリの「シグネチャー演出」
- 装飾は結果画面に集中させ、質問画面はシンプルに保つ（メリハリを付ける）

### アクセシビリティ
- キーボード操作で質問の選択肢を選べるようにする（tab/enter対応）
- `prefers-reduced-motion`を尊重し、アニメーションを軽減できるようにする
- 文字サイズ・コントラスト比はWCAG AA相当を目安に

---

## 5. 12タイプ定義（`src/data/ninjaTypes.js`のもと）

| id | タイプ名 | キーワード | 性格傾向 |
|---|---|---|---|
| komuso | 虚無僧 | 隠遁・静寂・内省 | 一人の時間を好み、感情を表に出さず物事を静かに観察する |
| shukke | 出家 | 悟り・無欲・奉仕 | 執着を捨て、人のために動くことに喜びを感じる |
| yamabushi | 山伏 | 修行・忍耐・自然 | 肉体と精神を鍛えることに価値を置き、困難を厭わない |
| akindo | 商人 | 社交・情報通・損得 | 人付き合いが得意で、交渉や駆け引きを楽しむ |
| hokashi | 放下師 | 芸・機転・身軽さ | 目立つことを恐れず、場を盛り上げるのが得意 |
| sarugakushi | 猿楽師 | 二面性・演技・仮面 | 状況に応じて自分を演じ分けるのが得意 |
| tsunenokatachi | 常の形 | 擬態・堅実・目立たなさ | 普段は目立たず、いざという時に力を発揮する |
| shikaku | 刺客 | 冷徹・単独・目的遂行 | 感情より目的を優先し、迷わず行動する |
| kanja | 間者 | 情報収集・洞察・裏工作 | 物事の裏側を読み取り、慎重に立ち回る |
| bushi | 武士 | 正義・名誉・正々堂々 | 正面から向き合うことを重んじ、筋を通す |
| kusuriya | 薬屋 | 実利・商才・駆け引き | リスクとリターンを見極め、実利を取りに行く |
| kusushi | 薬師 | 冷静・分析・専門知識 | 状況を分析し、知識で周囲を助ける役に回る |

各タイプには結果画面用に以下の情報も持たせる：
- `name`（表示名）／`nameEn`（ローマ字、ファイル名等に使用）
- `description`（診断結果として表示する2〜3文の説明文）
- `accentColor`（4章のイラスト背景色に対応する16進カラー）
- `imagePath`（8章の命名規則に対応する画像パス）

※説明文（description）は未作成。世界観に合わせて後日ライティングが必要
（希望があれば併せて作成可能）。

---

## 6. 質問設計（共通7問 + 系統別3問 = 全10問）

**診断は2段構成**。似た職業同士をまとめた「系統」をまず絞り込み、その系統に入った者
だけに専用の問いを出して、系統内のどのタイプかを決める。質問文・選択肢・配点は
「診断ロジック修正依頼書」の確定版に準拠している。変更する場合は必ず総当たりで
出現率を再確認し、この章と7章の実測値を更新すること。

- **共通7問**（`COMMON_QUESTIONS`）：全員に同じ4択を出し、5系統への加点で系統を決める。
  配点は「主系統+2点／副系統+1点」が基本だが、第1問Bのように1つの選択肢が
  2系統に+2ずつ加点する例外もある。第1問Aのみ主系統+1点（点数が軽い代わりに
  除外フラグを持つ。後述）。
- **系統別3問**（`BRANCH_QUESTIONS`）：系統が確定した後、その系統でしか出ない問いを3問出す。
  選択肢の数は系統内のタイプ数と一致する（3タイプ系統なら3択、2タイプ系統なら2択）。

### 系統の定義（`src/data/systems.js`）

| 系統 | 含まれるタイプ | 括り |
|---|---|---|
| 求道系 | 虚無僧・出家・山伏 | 世を捨て、己の内なる道を求める者たち |
| 武闘系 | 間者・刺客・武士 | 任務を果たすため、刃と知略を執る者たち |
| 芸能系 | 猿楽師・放下師 | 人目を引き、演じることを生業とする者たち |
| 医療系 | 薬師・薬屋 | 薬と知識で、人の生き死にに関わる者たち |
| 庶民系 | 常の形・商人 | 市井に紛れ、当たり前の顔で生きる者たち |

### 共通7問（系統を決める）

1. **どう潜入する？**
   A. 気配を断ち、闇に溶けて忍び込む（武闘+2／**除外フラグ：武士**）
   B. 何でもない顔で、堂々と正面から入り込む（武闘+3, 庶民+2）
   C. 芸や愛想を武器に、招かれるようにして入り込む（芸能+2, 庶民+1）
   D. 加持祈祷を捧げ、機が満ちるのを待つ（求道+2）

2. **潜入先で怪しまれそうになった。どうする？**
   A. その場に溶け込む（庶民+1, 求道+1）
   B. 大袈裟に笑い、芸を見せて気を逸らす（芸能+2）
   C. 堂々と名乗り、正面から相対する（武闘+2）
   D. 商売を通じて、役に立つ者として振る舞う（庶民+1, 医療+1）

3. **休息の日、何をして過ごす？**
   A. 山に入り、身を清めて鍛える（求道+2）
   B. 街へ出て、人と交わり噂を集める（庶民+2, 芸能+1）
   C. 芸や技を磨く（芸能+2）
   D. 薬草を摘み、調合を試す（医療+2）

4. **大切にしている信条は？**
   A. 忍び、ひたすらに耐え忍ぶこと（庶民+2）
   B. 九字を切り、心を鎮め澄ませること（求道+2）
   C. 摩利支天に祈り、姿を消して勝ちを得ること（武闘+2）
   D. 儒の教えに則り、正しき心のままに施すこと（医療+2）

5. **初対面の相手には、どう接する？**
   A. 多くを語らず、まず心の内で祈りを捧げる（求道+2）
   B. すぐに打ち解けるよう働きかける（庶民+2）
   C. 友人になりきり、心を開かせる（芸能+2）
   D. さりげなく様子を窺い、力になれることはないか考える（医療+2）

6. **仲間が窮地に陥った。どうする？**
   A. 我が身を顧みず、助けに向かう（武闘+3, 求道+1）
   B. 手当てをし、まず命を繋ぐ（医療+2）
   C. 場を茶化して空気を変え、隙を作る（芸能+2）
   D. 損得を計り、最も確実な手を打つ（庶民+2, 医療+1）

7. **任務の成否は、何で決まると思う？**
   A. 力と技の冴え（武闘+3）
   B. 積み重ねた鍛錬と、揺るがぬ信心（求道+2, 武闘+1）
   C. 場の空気を読む機転（芸能+2）
   D. 病や毒への備え（医療+2）

### 系統別3問（系統内のタイプを決める）

選択肢の並び順は `SYSTEMS[].typeIds` の順と一致させること（index をそのまま
タイプの指定に使っているため、順序がずれると別のタイプになる）。

- **求道系**（虚無僧／出家／山伏）：修行の場／心の鎮め方／悟りとは何か
- **武闘系**（間者／刺客／武士）：標的を前にどうする／任務が終わるのはいつか／闇で研ぐもの
- **芸能系**（猿楽師／放下師）：客の掴み方／芸の本質／素顔を見せるか
- **医療系**（薬師／薬屋）：薬を前に何を考えるか／銭のない病人への対応／薬とは何か
- **庶民系**（常の形／商人）：人混みでの自分／得意なこと／銭についての考え

---

## 7. 診断ロジック（`src/logic/scoring.js`）

### 第1段：系統の判定（`resolveSystem`）

1. 5系統分のスコアを0で初期化し、共通7問の回答ごとに加点
2. 最もスコアの高い系統を採用
3. **同点処理**：`SYSTEM_TIE_ORDER`で決める。質問数が少ないほど同点は起きやすくなるため、
   **最下位に置かれた系統は同点のたびに必ず負け、実出現率が想定以上に沈み込む**。
   この並びの目標は変遷している。
   - 「診断ロジック修正依頼書」で共通問を確定版の文言・配点に全面差し替えた際は
     5系統の均等化が目標で、`['kyudo','butou','iryo','geino','shomin']`
     （武士を除く最大/最小比1.375倍）を採用した。
   - 「修正依頼書：カテゴリー出現バランス調整」で目標が「武闘系が最も出やすく、
     庶民・芸能・求道・医療の4系統は僅差」に変わり、武闘系の得点を底上げ・
     庶民系の得点を微減する`scores`差分が入った。全120通りの並びを総当たりし、
     武闘系が常に最多になる並びの中から残り4系統のspread(最大/最小)が
     最小になる `['butou','shomin','kyudo','iryo','geino']`（spread 1.196倍）
     に差し替えた。
   質問文・配点や目標とする出現バランスを変更するたびに、同様の総当たりで
   最適な並びを確認すること

### 第2段：タイプの判定（`resolveType`）

1. 系統別3問で選んだ選択肢の index に対応するタイプへ、`branchWeights` の重みで加点
2. 最も点の高いタイプを採用
3. **同点処理**：「最後に選んだ側」を決め手にする。先頭固定にすると特定タイプが
   同点を総取りしてしまう（実際、3タイプ系統で虚無僧だけが26%まで跳ね上がった）

### 武士を最レアにする仕組み

武闘系の `branchWeights` は `[間者2, 刺客2, 武士1]`。武士の票だけ軽いため、
**3問すべてで武士側を選び通した者だけが武士に至る**（2問だけ選んでも間者か刺客になる）。
武士を単独の系統にする案もあったが、それだと系統内で分割されず逆に最も出やすく
なってしまうため、この形にしている。

### 除外フラグ（excludes）

選択肢の点数（`scores`）だけでは防げない「選んだ行動と結果のキャラクター設定が
矛盾する」パターンに対応する仕組み。共通問・系統別3問どちらの選択肢にも
`excludes: ['タイプID', ...]` を持たせられ、その選択をした回答者の最終候補から
該当タイプを除外する（系統の判定には影響しない。系統内で最終タイプを1つに
絞り込む段階でのみ効く）。除外の結果、系統内の候補が0件になる場合は
矛盾を避けるため除外を無視する（`resolveType`のフォールバック）。

現在の設定：第1問A「気配を断ち、闇に溶けて忍び込む」に
`excludes: ['bushi']`。正面から堂々と立ち合うことを信条とする「武士」と、
気配を消して隠密に立ち回るこの選択は矛盾するため。武闘系に決まった場合の
最終候補が「間者」「刺客」の2択に絞られる（第1問で他の選択肢を選んだ場合は
従来通り武士にも到達しうる）。

タイプを追加・変更した際は、`scoring.js`の開発時ガードが
「`excludes`に存在しないタイプIDがないか」「1つの選択肢が1系統の全タイプを
除外していないか（常にフォールバックして無意味になるため）」を自動検知する。

### 出現率（全パターン総当たりの実測値）

共通7問の 4^7=16384 通り × 各系統の分岐 3^3 または 2^3 通りを全探索。
系統ごとに分岐の組み合わせ数が違う（27通り対8通り）ので、
**1つの共通回答パス＝1.0 になるよう正規化してから集計すること**
（正規化を忘れると3タイプ系統が過大評価される）。

「カテゴリー出現バランス調整」以降は5系統の均等化ではなく、
**武闘系を意図的に最多にする**方針に変わっている。

系統レベル：武闘系36.15% ／ 庶民系17.58% ／ 芸能系16.06% ／ 求道系15.50% ／
医療系14.70%（武闘系を除く4系統のspreadは1.196倍）。

| タイプ | 出現率 | | タイプ | 出現率 |
|---|---|---|---|---|
| 間者 | 16.17% | | 薬師 | 7.35% |
| 刺客 | 15.69% | | 薬屋 | 7.35% |
| 常の形 | 8.79% | | 虚無僧 | 5.17% |
| 商人 | 8.79% | | 出家 | 5.17% |
| 猿楽師 | 8.03% | | 山伏 | 5.17% |
| 放下師 | 8.03% | | **武士** | **4.29%（最レア）** |

武闘系の間者・刺客が全体で最も出やすいタイプになった（系統自体を最多にした
狙いどおり）。武士は同じ武闘系の中でも間者・刺客より明確に低く、
除外フラグ（第1問Aを選ぶと武士が候補から消える）と系統別3問の分岐構造
（3問すべてで武士側を選ばないと到達しない）の二重の絞り込みで
最レアの座を維持している。
選択肢や配点を変えたら、必ず総当たりを回して
「出現しないタイプがないか」「武士が武闘系内で最下位のままか」「系統の同点処理で
特定の系統だけ不当に沈んでいないか」「除外フラグの追加でどのタイプの
出現率がどれだけ動いたか」を再確認すること。

## 8. アセット命名規則

`src/assets/characters/`配下に、5章の`id`列に対応する形で配置：

```
komuso.png
shukke.png
yamabushi.png
akindo.png
hokashi.png
sarugakushi.png
tsunenokatachi.png
shikaku.png
kanja.png
bushi.png
kusuriya.png
kusushi.png
```

イラストは別途Firefly / Nano Banana 2で生成済み（依頼書：
「忍者タイプ診断_イラスト指示書_Firefly_NanoBanana2版.md」参照）。
すべて縦長3:4、和紙風カード枠＋毛筆タイトル文字＋落款印入りの完成品を想定。

---

## 9. 実装の優先順位（MVP → 拡張）

**MVP（まずここまで）**
- [ ] StartScreen → QuestionScreen（10問）→ ResultCard の一直線フロー
- [ ] スコアリングロジックの実装
- [ ] 12タイプ分のイラスト差し込み
- [ ] レスポンシブ対応（スマホ優先）

**拡張（余力があれば）**
- [ ] 結果カードのSNSシェア（画像として書き出し／OGP対応）
- [ ] 診断結果の相性表示（自分のタイプと相性のいい/悪いタイプ）
- [ ] リトライ導線（もう一度診断する）
- [ ] 結果カードのダウンロード機能（`html2canvas`等）

---

## 10. 未確定事項（要相談）

- 12タイプそれぞれの`description`（結果文言）は未作成
- 質問の点数バランスは初期ドラフトのため、実装後の調整が前提
- SNSシェア機能を実装するかどうかは未定
