# 忍者タイプ診断アプリ — CLAUDE.md

このドキュメントはClaude Codeがこのプロジェクトを実装する際の指示書です。
「職業診断」形式の忍者版で、11問の質問に答えると12種類の忍者タイプのうち
どれに当てはまるかがカード形式で表示されるWebアプリを作ります。

---

## 1. プロジェクト概要

- **タイトル（仮）**：忍者タイプ診断
- **コンセプト**：一般的な「職業適性診断」の忍者版。前半6問（目立つ/目立たない
  の二択軸）＋後半5問（陣営確定後、その陣営の6タイプに対応する6択）の計11問に
  答えると、七方出（しちほうで）由来の7タイプ＋刺客・間者・武士・薬屋・薬師の
  計12タイプのうち1つが「診断結果カード」として表示される（6章）。各陣営に
  1タイプずつ「妥協のない極致」の激レアタイプが存在し、後半5問すべてをその
  タイプの選択肢で貫いた場合のみ到達する（7章）
- **トーン＆マナー**：和風・時代劇風。ふざけすぎず、少し中二病的な「らしさ」を楽しめる
  雰囲気（例：「お前は～の忍者だ」ではなく「お前の忍びの道は――**刺客**」のような演出）
- **ホスティング**：無料枠（Vercel / Netlify等）を前提。バックエンド不要、
  全てクライアントサイドで完結させる

---

## 2. 技術スタック

- **フレームワーク**：React + Vite
- **スタイリング**：CSS Modules または通常のCSS（Tailwindは使わず、和風の世界観を
  トークンベースの独自CSSで作り込む方針。詳細は4章）
- **状態管理**：React標準の`useState`/`useReducer`で十分（Redux等は不要な規模）
- **アニメーション**：CSSトランジション＋必要に応じて`Framer Motion`
  （結果カードのめくり演出など、演出の質を上げたい箇所に限定して使用）
- **画像アセット**：`/src/assets/characters/`配下にタイプごとのイラストを配置
- **バックエンド**：なし。診断ロジック・結果表示はすべてフロントエンドで完結
- **デプロイ**：Vercel想定（`vite build`の成果物をそのままデプロイ）

---

## 3. ディレクトリ構成（提案）

```
ninja-shindan/
├── CLAUDE.md
├── README.md
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── data/
│   │   ├── ninjaTypes.js       # 12タイプの定義（5章）
│   │   ├── factions.js         # 2陣営(目立つ/目立たない)の定義と所属タイプ（6章）
│   │   └── questions.js        # 前半5問 + 後半5問（6章）
│   ├── components/
│   │   ├── StartScreen.jsx        # タイトル・診断開始画面
│   │   ├── QuestionScreen.jsx     # 前半問の表示・選択肢UI
│   │   ├── FactionQuestionScreen.jsx # 後半問（陣営名の帯つき、6択）
│   │   ├── ProgressBar.jsx        # 「第◯問」の進捗表示
│   │   ├── ResultCard.jsx         # 診断結果カード（掛軸仕立て）
│   │   ├── TypeGallery.jsx        # 結果画面下の十二タイプ一覧（陣営別）
│   │   └── decorations.jsx        # 手裏剣・題字プレート等の装飾パーツ
│   ├── logic/
│   │   └── scoring.js          # 陣営判定＋陣営内タイプ判定（7章）
│   ├── styles/
│   │   ├── tokens.css          # デザイントークン（4章）
│   │   └── global.css
│   └── assets/
│       └── characters/         # 12タイプのイラスト（8章の命名規則で配置）
```

---

## 4. デザイン方針

このアプリ固有の「和風・時代劇画×モダンアニメ」の世界観を、AIにありがちな
テンプレ配色（生成りベース＋テラコッタ／黒背景＋ネオン一色）に寄せず、
イラストの世界観（藍・墨・紅・金）から直接トークンを起こす。

### カラートークン
```css
:root {
  --color-sumi:        #1A1A22; /* 墨・base背景（濃紺寄りの黒） */
  --color-washi:       #EDE6D6; /* 生成り・カード背景、和紙 */
  --color-beni:        #B23A2E; /* 紅・落款印、アクセント（CTAボタン等） */
  --color-kondo:       #C9A227; /* 金泥・タイトルの縁取り、演出の輝き */
  --color-ainezu:      #3E4A5C; /* 藍鼠・サブテキスト、枠線 */
  --color-text-light:  #F4F0E6; /* 濃色背景上の文字 */
  --color-text-dark:   #221F1A; /* 淡色背景上の文字 */
}
```
タイプごとの識別色は各イラストの背景色（虚無僧＝青灰、出家＝琥珀、山伏＝山緑…）
をそのまま結果カードのアクセントカラーとして流用し、統一感を持たせる。

### タイポグラフィ
- **見出し・タイプ名**：明朝体の毛筆寄りフォント（例：`Shippori Mincho`
  または`Zen Old Mincho`）。診断結果のタイプ名はこのフォントを大きく使い、
  「筆文字が浮かび上がる」演出の主役にする
- **本文・質問文**：可読性重視のゴシック体（例：`Zen Kaku Gothic New`
  または`Noto Sans JP`）
- どちらもGoogle Fontsで無料利用可能

### レイアウト・演出方針
- 1画面1質問のシングルカラム構成。スマホでの利用を主軸に設計
- 質問→回答のたびに軽いフェード/スライド遷移
- **結果画面が最大の見せ場**：暗転→和紙が広がるような背景→イラストがフェードイン
  →タイトル文字が毛筆で書かれるように現れる→最後に落款印が「ポン」と押される
  演出（Framer Motionのタイミング制御で実現）。これが本アプリの「シグネチャー演出」
- 装飾は結果画面に集中させ、質問画面はシンプルに保つ（メリハリを付ける）

### アクセシビリティ
- キーボード操作で質問の選択肢を選べるようにする（tab/enter対応）
- `prefers-reduced-motion`を尊重し、アニメーションを軽減できるようにする
- 文字サイズ・コントラスト比はWCAG AA相当を目安に

---

## 5. 12タイプ定義（`src/data/ninjaTypes.js`のもと）

| id | タイプ名 | キーワード | 性格傾向 |
|---|---|---|---|
| komuso | 虚無僧 | 隠遁・静寂・内省 | 一人の時間を好み、感情を表に出さず物事を静かに観察する |
| shukke | 出家 | 悟り・無欲・奉仕 | 執着を捨て、人のために動くことに喜びを感じる |
| yamabushi | 山伏 | 修行・忍耐・自然 | 肉体と精神を鍛えることに価値を置き、困難を厭わない |
| akindo | 商人 | 社交・情報通・損得 | 人付き合いが得意で、交渉や駆け引きを楽しむ |
| hokashi | 放下師 | 芸・機転・身軽さ | 目立つことを恐れず、場を盛り上げるのが得意 |
| sarugakushi | 猿楽師 | 二面性・演技・仮面 | 状況に応じて自分を演じ分けるのが得意 |
| tsunenokatachi | 常の形 | 擬態・堅実・目立たなさ | 普段は目立たず、いざという時に力を発揮する |
| shikaku | 刺客 | 冷徹・単独・目的遂行 | 感情より目的を優先し、迷わず行動する |
| kanja | 間者 | 情報収集・洞察・裏工作 | 物事の裏側を読み取り、慎重に立ち回る |
| bushi | 武士 | 正義・名誉・正々堂々 | 正面から向き合うことを重んじ、筋を通す |
| kusuriya | 薬屋 | 実利・商才・駆け引き | リスクとリターンを見極め、実利を取りに行く |
| kusushi | 薬師 | 冷静・分析・専門知識 | 状況を分析し、知識で周囲を助ける役に回る |

各タイプには結果画面用に以下の情報も持たせる：
- `name`（表示名）／`nameEn`（ローマ字、ファイル名等に使用）
- `description`（診断結果として表示する2〜3文の説明文）
- `accentColor`（4章のイラスト背景色に対応する16進カラー）
- `imagePath`（8章の命名規則に対応する画像パス）

※説明文（description）は未作成。世界観に合わせて後日ライティングが必要
（希望があれば併せて作成可能）。

---

## 6. 質問設計（前半6問 + 後半5問 = 全11問）

**診断は2段構成**。「忍者タイプ診断_新ロジック仕様書」に基づく全面リニューアル版
（旧「診断ロジック修正依頼書」の除外フラグ方式・旧「修正依頼書：カテゴリー出現
バランス調整」の5系統ポイント制は、いずれも本仕様に完全に置き換えられ無効）。

- **前半6問**（`FRONT_QUESTIONS`）：全員に同じ4択を出す。各問「目立つ+2」の
  選択肢2つ／「目立たない+2」の選択肢2つで構成。6問終了時点でスコアの高い方の
  陣営（6タイプ）に確定する。
- **後半5問**（`BACK_QUESTIONS`）：陣営が確定した後、その陣営の6タイプに対応する
  6択を5問出題する。選んだ分だけそのタイプに+1する（等ウェイト）。

### 陣営の定義（`src/data/factions.js`）

| 陣営 | 含まれるタイプ | 括り |
|---|---|---|
| 目立つ陣営 | 放下師・猿楽師・商人・薬屋・山伏・**武士（激レア）** | 目立つことこそが最良の隠れ蓑と心得る者たち |
| 目立たない陣営 | 虚無僧・出家・常の形・間者・**刺客（激レア）**・薬師 | 気配を消すことにこそ己の道を見出す者たち |

各陣営には「妥協のない極致」として1タイプずつ激レアが存在する
（目立つ陣営＝武士、目立たない陣営＝刺客）。詳細は7章。

後半5問の画面には、当初「見極めるは／○○陣営／summary」という帯を表示していたが、
ネタバレ感が強すぎるとのフィードバックで削除した（`FactionQuestionScreen.jsx`）。
陣営名は結果画面（`ResultCard.jsx`の`result-card__eyebrow`）で初めて明かされる。

### 前半6問（陣営を決める）

「最後まで分からないように」という要望に基づき、以下の工夫をしている。

- **選択肢は行動の描写に留める**：「目立つ」「気配を消す」と直接言い切らず、
  具体的な行動で軸をにじませる。旧5系統版の質問集の文言を極力流用しており、
  各選択肢がもともと想定していたタイプ（例:「気配を殺し、遠間から標的を
  見定める」なら刺客）を読み取り、そのタイプが新方式のどちらの陣営に属するかで
  軸を再割り当てした（旧5系統の枠組みとは系統単位で綺麗に対応しないため、
  選択肢単位で判定している）。
- **忍者の職務だけに偏らせない**：任務・潜入系の質問が並ぶと「これは行動適性
  診断だ」と気づかれやすいため、第5問に日常の場面（贈り物選び）を挟んで
  毛色を変えている。
- **4択が機械的に2:2で読めないようにする**：一部の選択肢は両軸に+2する
  「中立」の選択肢にしてある（第4問A「瞑想して過ごす」）。中立の選択肢を
  混ぜると前半の対称性が崩れるため、必ず総当たりで7章の実測値を更新すること
  （実際、この変更で目立つ48.9%→46.9%まで動いた）。

1. **任務を与えられた。まず何をする？**
   A. 皆の前で役割を言い渡し、士気を高める（目立つ）
   B. 一人で黙々と、身支度と得物を整える（目立たない）
   C. まず情報を集めようと、あちこちに顔を出す（目立つ）
   D. 気配を殺し、誰にも知られぬ内に動き出す（目立たない）

2. **どう潜入する？**
   A. 声をかけながら、堂々と門をくぐる（目立つ）
   B. 誰かの陰に紛れ、そっと敷地に入り込む（目立たない）
   C. 話題を振りまき、注意を逸らしながら進む（目立つ）
   D. 足音を殺し、闇と一つになって進む（目立たない）

3. **初対面の相手には、どう接する？**
   A. すぐに打ち解けるよう働きかける（目立つ）
   B. 多くを語らず、まず心の内で祈りを捧げる（目立たない）
   C. 友人になりきり、心を開かせる（目立つ）
   D. さりげなく様子を窺い、力になれることはないか考える（目立たない）

4. **休息の日、何をして過ごす？**
   A. 瞑想して過ごす（目立つ・目立たない両方に加点する中立の選択肢）
   B. 人知れず、市中の噂の出所を探る（目立たない）
   C. 芸や技を、人前で磨く（目立つ）
   D. 薬草を摘み、静かに調合を試す（目立たない）

5. **贈り物を選ぶなら、どうする？**（忍者の職務と無関係な質問）
   A. 皆で盛り上がれる、賑やかな品を選ぶ（目立つ）
   B. 相手の心にそっと寄り添う品を選ぶ（目立たない）
   C. 誰もが目を引く、とびきりの品を探す（目立つ）
   D. 誰にも気づかれぬよう、そっと届ける（目立たない）

6. **潜入先で怪しまれそうになった。どうする？**
   A. 大袈裟に笑い、芸を見せて気を逸らす（目立つ）
   B. その場に溶け込む（目立たない）
   C. 堂々と名乗り、正面から相対する（目立つ）
   D. 音もなく身を引き、姿をくらます（目立たない）

### 後半5問（陣営内のタイプを決める）

選択肢の並び順は `FACTIONS[].typeIds` の順（A〜F）と一致させること（index を
そのままタイプの指定に使っているため、順序がずれると別のタイプになる）。

問1のみ、前半→後半の切り替わりを滑らかにするため、**問い文を両陣営で共通**にしている
（「お前が最も"己"を出せるのは、どんな時だ？」）。選択肢の中身はそれぞれの陣営のまま。
質問文だけを見て陣営が確定した瞬間だと気づかれにくくする狙い。

- **目立つ陣営**（放下師／猿楽師／商人／薬屋／山伏／武士）：己を出せる時（共通問1）／
  目立ち方の本質／人々から求められるもの／一番大切にしているもの／素顔を見せるか
- **目立たない陣営**（虚無僧／出家／常の形／間者／刺客／薬師）：己を出せる時（共通問1）／
  隠れ方の本質／人からどう思われたいか／一番大切にしているもの／"消える"とは何か

質問文・選択肢を変更するたびに、必ず総当たりで7章の実測値を更新すること。

## 7. 診断ロジック（`src/logic/scoring.js`）

### 第1段：陣営の判定（`resolveFaction`）

1. 「目立つ」「目立たない」のスコアを0で初期化し、前半6問の回答ごとに加点
   （通常の選択肢は片方に+2、中立の選択肢は両方に+2）
2. 高い方の陣営を採用
3. **同点処理**：第1問で選んだ軸で決める（`resolveFaction`の第2引数、
   `dominantAxis`で選択肢のscoresから求める）。前半を5問(奇数)→6問(偶数)に
   増やした際、3:3の同点が数学的に起こりうる構成に変わった。全4,096通りを
   総当たりしたところ960通りが同点になり、いずれも第1問の軸できちんと決着
   することを確認済み。

**均等性への影響**：第4問Aを中立の選択肢（両軸+2）にしたことで、前半の対称性が
崩れている。全4,096通りの総当たりで目立つ1,920／4,096（46.88%）・
目立たない2,176／4,096（53.13%）とやや目立たない寄りに傾いた（「あからさまさを
消す」ための意図的な改変の直接的な結果であり、想定内）。均等性より不自然さの
排除を優先した判断のため、この程度のずれは許容している。選択肢を変更するたびに
必ず総当たりで数値を更新すること。

### 第2段：タイプの判定（`resolveType`）

1. 後半5問で選んだ選択肢のtypeIdへ、1票ずつ加点（等ウェイト）
2. 最も票の多いタイプを採用
3. **同点処理**：「最後に選んだ側」を決め手にする（これまでの分岐判定と同じ方針。
   先頭固定にすると特定タイプが同点を総取りしてしまうため）

### 武士・刺客を最レア（激レア）にする仕組み

各陣営の`extremeTypeId`（目立つ陣営＝武士、目立たない陣営＝刺客）は、
**後半5問すべてをそのタイプの選択肢で貫いた場合のみ**結果になる。

- 1問でも他のタイプを選んだ時点で、そのタイプは候補から完全に除外される
  （「5問中4問だけ武士」のような惜しい結果は存在しない、白か黒かの仕様）
- 激レアタイプへの回答は「無効票」として扱われる（他のどのタイプにもカウント
  されない）。そのため後半5問のうち複数問で激レアタイプを選んだが最終的に
  外れた人は、残り4問（またはそれ以下）の中での最多得票で決まる

### 出現率（全パターン総当たりの実測値）

前半6問は4^6=4,096通り、後半5問は各陣営6^5=7,776通りを全探索。
前半と後半は独立している（前半はどちらの陣営になるかだけを決め、後半の
分布は前半の回答内容に左右されない）ため、陣営ごとに後半だけを総当たりすれば
実測値が求まる。

**陣営レベル**：目立つ1,920／4,096（46.88%）／目立たない2,176／4,096（53.13%）。
第4問Aを中立の選択肢にした影響でやや目立たない寄りに傾いている（7章参照）。

**目立つ陣営（7,776通り）**

| タイプ | 出現率 |
|---|---|
| 放下師 | 20.00% |
| 猿楽師 | 20.00% |
| 商人 | 20.00% |
| 薬屋 | 20.00% |
| 山伏 | 20.00% |
| **武士** | **0.0129%（1/7,776。激レア）** |

**目立たない陣営（7,776通り）**

| タイプ | 出現率 |
|---|---|
| 虚無僧 | 20.00% |
| 出家 | 20.00% |
| 常の形 | 20.00% |
| 間者 | 20.00% |
| 薬師 | 20.00% |
| **刺客** | **0.0129%（1/7,776。激レア）** |

激レアを除く5タイプは各陣営内で完全に同率（1.000倍）。等ウェイト・
全パターンで作用する「最後に選んだ側」の同点処理と、陣営内タイプ数の対称性
（6択・1タイプ=1票）により、意図的な調整をしなくても構造的に均等になる。
激レア2タイプ（武士・刺客）は「後半5問すべてをそのタイプで貫く」という
唯一の到達条件により、他10タイプと隔絶した出現率になっている（仕様通り）。

選択肢や配点を変えたら、必ず総当たりを回して「出現しないタイプがないか」
「激レア以外の5タイプが均等のままか」「武士・刺客が引き続き激レアか」を
再確認すること。

## 8. アセット命名規則

`src/assets/characters/`配下に、5章の`id`列に対応する形で配置：

```
komuso.png
shukke.png
yamabushi.png
akindo.png
hokashi.png
sarugakushi.png
tsunenokatachi.png
shikaku.png
kanja.png
bushi.png
kusuriya.png
kusushi.png
```

イラストは別途Firefly / Nano Banana 2で生成済み（依頼書：
「忍者タイプ診断_イラスト指示書_Firefly_NanoBanana2版.md」参照）。
すべて縦長3:4、和紙風カード枠＋毛筆タイトル文字＋落款印入りの完成品を想定。

---

## 9. 実装の優先順位（MVP → 拡張）

**MVP（まずここまで）**
- [ ] StartScreen → QuestionScreen（10問）→ ResultCard の一直線フロー
- [ ] スコアリングロジックの実装
- [ ] 12タイプ分のイラスト差し込み
- [ ] レスポンシブ対応（スマホ優先）

**拡張（余力があれば）**
- [ ] 結果カードのSNSシェア（画像として書き出し／OGP対応）
- [ ] 診断結果の相性表示（自分のタイプと相性のいい/悪いタイプ）
- [ ] リトライ導線（もう一度診断する）
- [ ] 結果カードのダウンロード機能（`html2canvas`等）

---

## 10. 未確定事項（要相談）

- 12タイプそれぞれの`description`（結果文言）は未作成
- 質問の点数バランスは初期ドラフトのため、実装後の調整が前提
- SNSシェア機能を実装するかどうかは未定
